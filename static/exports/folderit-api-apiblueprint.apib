FORMAT: 1A
HOST: https://api.folderit.com

# Folderit API
# Changes

### 2025.08.24 - 2.0.71
* Permission properties now support narrowing with the `fields` parameter.

### 2025.08.22 - 2.0.70
* Automatically expand requested fields, expand parameter is not required for the fields listed under the expanded variants
* Add `thumbnails` property to File and FileVersion schemas

### 2025.08.21 - 2.0.69
* Add extension filter to [entities by parent](#tag/Entity/operation/getEntitiesByParent), [entities by tag](#tag/Entity/operation/getEntitiesByTag) and [all entities](#tag/Entity/operation/getAllEntities)

### 2025.08.12 - 2.0.68
* Add `parentUid` query parameter to [Recycled entities](#tag/Entity/operation/getRecycledEntities)

### 2025.08.11 - 2.0.67
* Add `position` as an expandable field to a team user

### 2025.08.10 - 2.0.66
* Alter [Team users](#tag/Account/operation/getTeamUsers) endpoint to a paginated response, add groups as an expandable field to a team user

### 2025.07.31 - 2.0.65
* Add lockId to [Lock a file](#tag/File/operation/lockFilePost) request  
  Unlocking is supported only with a [PUT request](#tag/File/operation/unlockFile2)

### 2025.07.29 - 2.0.64
* Add teamUserStatus field to [account users](#tag/Account/operation/getAccountUsers) and status field to [account team users](#tag/Account/operation/getTeamUsers)

### 2025.07.24 - 2.0.63
* Add an optional operator to the search filter filterItem->number

### 2025.07.01 - 2.0.62
* Add account stats expandable field

### 2025.06.10 - 2.0.61
* Add paginated response variants to [reminder listings](#tag/Reminder)
* Add recurrence property to [update a Reminder](#tag/Reminder/operation/updateReminder)

### 2025.06.04 - 2.0.60
* Add a method to [empty the recycle bin](#tag/Entity/operation/emptyRecycleBin)

### 2025.06.03 - 2.0.59
* Add the size property to [file versions](#tag/File/operation/getFileVersions)
* Add a paginated response variant to [file versions listing](#tag/File/operation/getFileVersions)
* Fetch multiple entities by UID at [Sections/entities](#tag/Entity/operation/getEntities)
* [Duplication of entities](#tag/Entity/operation/duplicateEntities)
* Add a paginated response variant to [related entities listing](#tag/Entity/operation/getEntityRelatedItems)

### 2025.05.26 - 2.0.58
* Add operationId to all paths

### 2025.04.17 - 2.0.57
* Add method property to resolution workflow audit trail data property

### 2025.04.06 - 2.0.56
* Add 2 new paths for fetching tags, add pagination and sorting to existing tag listing
  * [Get only root tags](#tag/Tag/operation/getRootTags) 
  * [Get sub-tags](#tag/Tag/operation/getSubTags) 
* Add [Search entities](#tag/Search/operation/searchEntities) endpoint

### 2025.04.02 - 2.0.55
* Add [resolutions](#tag/Resolution) and [workflows](#tag/Workflow)
* Add resolutionSupport extendable property to Entity

### 2025.03.27 - 2.0.54
* Add `options` as an expandable field to Meta

### 2025.03.26 - 2.0.53
* Add [entities by tag](#tag/Entity/operation/getEntitiesByTag) endpoint

### 2025.02.03 - 2.0.52
* Add more granular permissions for account sharing

### 2025.01.20 - 2.0.51
* Add `recurrence` property to reminders

### 2025.01.17 - 2.0.50
* Add `time` property to file lock requests for custom lock duration

### 2024.12.09 - 2.0.49
* Add `source` field to retention for customizing the source attribute for calculating the retention end time

### 2024.11.13 - 2.0.48
* Add `accountWatermarking` audit trail event

### 2024.11.13 - 2.0.47
* Add `timeFormat` as expandable field to User

### 2024.10.25 - 2.0.46
* Added comment field for workflow responses in audit trail

### 2024.10.10 - 2.0.45
* Added accountTeamUserManagedInvite, accountTeamUserManagedInviteDelete, accountTeamUserManagedAccept and accountTeamUserManagedReject audit trail events

### 2024.09.17 - 2.0.44
* Deprecate Account file report [`GET /v2/accounts/{accountUid}/reports/file-list`](#tag/File/operation/getAccountFileReport)

### 2024.09.10 - 2.0.43
* Meta of type `float` is renamed from Float to Decimal, type field stays the same for compatibility.  
  Value is represented as a string for compatibility with higher precisions and larger values.
* Change asic entity type text representation from `asic` to `file`
* Add prefix and wildcard search operators for meta filters

### 2024.09.02 - 2.0.42
* Add `language` to the composable filter of search

### 2024.09.02 - 2.0.41
* Add an expandable field `activeUser` to audit trails. Present, when a substitute user performed the action.

### 2024.07.26 - 2.0.40
* File and folder retention audit events contain no `data` property any more on removal of retention
* Add `cause` to audit trails to mark actions performed by background processes. Initially the only option is `retention`.
* Add all entities listing endpoint: [`GET /v2/accounts/{accountUid}/entities/all`](#tag/Entity/operation/getAllEntities)

### 2024.06.14 - 2.0.39
* Mark inbox and team folders as nullable fields for Account

### 2024.05.27 - 2.0.38
* Fix Audit trail sync-log id field value
* Add `sort` and `viewType` expandable fields to Folder

### 2024.05.23 - 2.0.37
* Search: add `uid` to queryFilter list 

### 2024.04.26 - 2.0.36
* Add `visibleColumnsWithMeta` as expandable field to Folder to get meta columns with names

### 2024.04.18 - 2.0.35
* Add `remoteUsername` as expandable field to Team user

### 2024.04.16 - 2.0.34
* Add redirect Location url in content as well
* Add `creator` as expandable field to Entity
* Include response samples with all expanded fields.  
  Note: It is recommended to ask for as few fields as is necessary as there are optimizations in place in the background for generating responses. This goes for both default fields and extra related data using expand option. For example, when requesting for all entities under a parent with tags, meta etc, if You don't need to have permissions for each related piece, do not request for them. A sample get param would be: `?fields=uid,name,tags.uid,tags.name,meta.meta.uid,meta.value&expand=tags,meta:myMetaUid,meta:myOtherMetaUid`

### 2024.04.11 - 2.0.33
* Add sorting to entities by parent endpoint: [`GET /v2/accounts/{accountUid}/entities/{entityUid}/entities`](#tag/Entity/operation/getEntitiesByParent)
* Add `extension` as expandable field to Entity
* Add `size` as expandable field to Entity

### 2024.04.10 - 2.0.32
* Update folder column attribute options list
* Add `tags` as expandable field to Entity
* Add `dateFormat` as expandable field to User

### 2024.03.20 - 2.0.31
* Add `meta:UID` expand field to entities

### 2024.03.18 - 2.0.30
* Get a list of parents for an entity: [`GET /v2/accounts/{accountUid}/entities/{entityUid}/parents`](#tag/Entity/operation/getEntityParents)
* Add isRoot property to entity

### 2024.03.11 - 2.0.29
* Add sectionOrder property to entity and folder models (applicable to only root entities/sections)
* Add folderStats property to entity and folder models
* Add inboxFolder and teamFolder properties to entity and folder models

### 2024.01.09 - 2.0.28
* Add account as an expandable relation to Entity

### 2023.11.24 - 2.0.27
* Add highlight expand field for search results

### 2023.11.22 - 2.0.26
* Add `version` expandable field to `File` objects

### 2023.10.30 - 2.0.25
* Add fileUpdateContent and fileUpdateOnline permissions to file
* Add canShareCustomPermissions to Account permissions list
  * Without this permission, You can only set the role property for a share, no settings

### 2023.10.22 - 2.0.24
* Add endpoints for getting shares to a specific user
  * Get direct shares to a specific user: [`GET /v2/accounts/{accountUid}/users/{userUid}/shares`](#tag/Share/operation/getUserShares)
  * Get all shares (including through group memberships) accessible by a specific user: [`GET /v2/accounts/{accountUid}/users/{userUid}/access`](#tag/Share/operation/getAllUserShares)
* Add documentation for fetching share info: [`GET /v2/accounts/{accountUid}/shares/{shareUid}`](#tag/Share/operation/getShare)
* Add settings property to shares

### 2023.10.18 - 2.0.23
* Add missing endpoint documentation for tag sharing
  * Get tag shares: [`GET /v2/accounts/{accountUid}/tags/{tagUid}/shares`](#tag/Tag/operation/getTagShares)
  * Share a tag: [`POST /v2/accounts/{accountUid}/tags/{tagUid}/shares`](#tag/Tag/operation/addTagShares)

### 2023.10.18 - 2.0.22
* Add group expand parameters to doc

### 2023.09.20 - 2.0.21
* Add composable filter and scoring option to file/folder searches
  * *filter* property can contain a nested set of familiar filters to create a required composite set of filters
  * *scoring* property is used to force scoring even when we're not doing search by query directly (only filtering)  
    Scoring results are not guaranteed to be sensible

### 2023.09.19 - 2.0.20
* Added fileOnlineView and fileOnlineEdit audit trail events

### 2023.09.14 - 2.0.19
* Sections list is now available for partially accessible accounts, canGetSections property indicates that all sections are guaranteed to be accessible
* Share items to user by userUid

### 2023.09.03 - 2.0.18
* Enable moving tags around

### 2023.08.30 - 2.0.17
* Add `isUsed` attribute to Meta Select Option
* Add tag management to entities for a single point to manage tags for all types of entities
* Tag sharing
* Add tagUid filter to file/folder search endpoints

### 2023.08.10 - 2.0.16
* Add searchAfter field to file/folder search endpoints for paging over more than 10000 results.

### 2023.07.31 - 2.0.15
* Add sorting to file/folder search endpoints

### 2023.03.05 - 2.0.14
* Reminders for all types of entities
  * Added two endpoints, one specifically for folders, other for any type of entity
  * Folders: [`GET /v2/accounts/{accountUid}/folders/{folderUid}/reminders`](#tag/Reminder/operation/getFolderReminders)
  * Entities: [`GET /v2/accounts/{accountUid}/entities/{entityUid}/reminders`](#tag/Reminder/operation/getEntityReminders)
* Related items to all types of entities of all types  
  * Added two endpoints, one specifically for folders, other for any type of entity
  * Folders: [`GET /v2/accounts/{accountUid}/folders/{folderUid}/related`](#tag/Folder/operation/getFolderRelatedItems)
  * Entities: [`GET /v2/accounts/{accountUid}/entities/{entityUid}/related`](#tag/Entity/operation/getEntityRelatedItems)
* Meta for all items, new endpoint is under entities: [`GET /v2/accounts/{accountUid}/entities/{entityUid}/meta`](#tag/Entity/operation/getEntityMeta)

### 2023.02.21 - 2.0.13
* Add new request header `X-Folderit-Request-Key` to make it possible to differentiate audit trails resulting from your own requests
* Add audit trail endpoint tailored for file syncing: [`GET /v2/accounts/{accountUid}/audit/syncLog`](#tag/Audit/operation/getFileSyncLog)
* Add `extra` field to the expand list of folder and entity, which gives access to number, note, date etc.
* Add `extra` field to folder and entity update endpoints

### 2023.01.23 - 2.0.12
* Add includeFiles, includeLinks and includeFolders fields for FolderMeta

### 2022.07.22 - 2.0.11
* Add groups as group members ([GET group all members](#operation/getGroupMembers))
* Add [Entity endpoint](#tag/Entity) for a unified access to directory structure (access to all types of entities like folders, files, links and more in the future)

### 2022.04.13 - 2.0.10
* Add `fields` and `expand` query parameters to paths resulting in accounts, folders and files
  * `fields` parameter can be used to reduce amount of data returned. For example: `fields=uid,name`
  * `expand` parameter can be used to fetch related objects with the request, for example fetching meta with a file list.
    * [`GET /v2/accounts/{accountUid}/folders/{folderUid}/files?expand=meta,people,tags`](#tag/Folder/operation/getFilesByParent) includes meta, people and tags arrays with each file
  * `expand` and `fields` can be combined to get all fields of the initial object, but only a few from a related object:
    * `fields=*,meta.value,meta.meta.name&expand=meta`
* Search result updates for autocomplete [`GET /v2/accounts/{accountUid}/search/simple`](#tag/Search/operation/searchAutocomplete) and [`GET /v2/accounts/{accountUid}/search/files`](#tag/Search/operation/searchFiles):
  * Mark file search result pathUid property as deprecated
  * Add mimeType property to file search results

### 2022.04.06 - 2.0.9
* Add email as a required field for signing up team users via [`POST /v2/accounts/{accountUid}/team-users`](#tag/Account/operation/addTeamUser)

### 2022.04.02 - 2.0.8
* Add new audit events
  * fileDuplicate
  * folderDuplicate
  * folderSubfileDuplicate
  * folderSubfolderDuplicate
  * accountSectionDuplicate
* Add userUid and groupUid to Audit Trail Share data
* Audit trail fileDownload events now have fileVersionUid data

### 2022.02.23 - 2.0.7
* Add folder import emails (mailInAddress)
  * Activate an address for importing mails to any folder (in contrast to Inbox section, which always has a static address)
    * Activate address: [`POST /v2/accounts/{accountUid}/folders/{folderUid}/email`](#tag/Folder/operation/configureFolderEmail)
    * Deactivate existing address: [`DELETE /v2/accounts/{accountUid}/folders/{folderUid}/email`](#tag/Folder/operation/deleteFolderEmail)
    * Reset (active or deleted) address to get a new random key: [`PUT /v2/accounts/{accountUid}/folders/{folderUid}/email`](#tag/Folder/operation/requestNewFolderEmailAddress)
  * If You prefer to only use POST requests, use `action` parameter in request body

### 2022.02.13 - 2.0.6
* Add dateFrom and dateTo filters for file search
* Add file report [`GET /v2/accounts/{accountUid}/reports/file-list`](#tag/File/operation/getAccountFileReport)

### 2022.01.24 - 2.0.5
* Add new audit event: fileResolutionDelete

### 2022.01.22 - 2.0.4
* Adjusted field names in error responses to correct names

### 2022.01.16 - 2.0.3
* Added pagination
  * [`GET /v2/accounts/{accountUid}/folders/{folderUid}/folders`](#tag/Folder/operation/getFolderByParent)
  * [`GET /v2/accounts/{accountUid}/recycle-bin/folders`](#tag/Folder/operation/getRecycledFolders)
  * [`GET /v2/accounts/{accountUid}/recycle-bin/files`](#tag/File/operation/getRecycledFiles)

### 2022.01.14 - 2.0.2
* Fix Folder visibleColumns meta column spec

### 2022.01.14 - 2.0.1
* Added `page-size` parameter to paths that already had `page` parameter
  * [`GET /v2/accounts/{accountUid}/audit/accountLog`](#tag/Audit/operation/getAccountRecentAuditTrail)
  * [`GET /v2/accounts/{accountUid}/audit`](#tag/Audit/operation/getAccountAuditTrail)
  * [`GET /v2/accounts/{accountUid}/files/{fileUid}/audit`](#tag/Audit/operation/getFileAuditTrail)
  * [`GET /v2/accounts/{accountUid}/folders/{folderUid}/audit`](#tag/Audit/operation/getFolderAuditTrail)
  * [`GET /v2/accounts/{accountUid}/groups`](#tag/Group/operation/getGroups)
* Added pagination
  * [`GET /v2/accounts/{accountUid}/folders/{folderUid}/files`](#tag/File/operation/getFilesByParent)
* Added missing pagination documentation
  * [`GET /v2/accounts/{accountUid}/groups/{groupUid}/users`](#tag/Group/operation/getGroupUsers)

### 2021.12.19 - 2.0.0
API v2 comes with multiple breaking changes. Most notably:
* Removed any reference to object `ID`s. Instead we are now using `UID`s. UIDs are globally unique and have a data type of CHAR(10)
* Paths now include `accountUid` as required parameter
* `accountId` paramater has been removed from request body parameters as `accountUid` is now always part of the path parameters for Account-related requests
* New tags and people/signees can be created directly without attaching them to a file from [`POST /v2/accounts/{accountUid}/tags`](#tag/Tag/operation/addTag) and [`POST /v2/accounts/{accountUid}/people`](#tag/Person/operation/addPerson)
* Removed deprecated paths and parameters

#### New features
* Audit trail: added new tag and person/signee events on Account level
  * tagNew
  * tagDelete
  * tagName
  * signeeNew
  * signeeDelete
  * signeeName
* Search
  * Added queryFilter options to File search
    * number
    * mailSubject
    * mailSenderEmail
    * mailSenderName
    * mailContent
  * Added filter fields to File search
    * number
    * mailSubject
    * mailSenderEmail
    * mailSenderName
    * mailContent

#### Detailed changes to paths:
* Removed paths:
  * /files
  * /files/{fileId}
  * /folders
  * /folders/{folderId}
  * /accounts/{accountId}/recycle-bin
  * /meta/{metaId}
  * /files/search
  * /folders/search
  * /download
  * /audit/files/{fileId}
  * /audit/folders/{folderId}
* New paths:
  * /v2/accounts/{accountUid}/files/{fileUid}/preview
  * /v2/accounts/{accountUid}/files/{fileUid}/versions/{fileVersionUid}/download
* Changed paths:
  * /files/upload -> /v2/accounts/{accountUid}/files/upload
  * /files/single/{fileId} -> /v2/accounts/{accountUid}/files/{fileUid}
  * /files/{fileId}/permissions -> /v2/accounts/{accountUid}/files/{fileUid}/permissions
  * /files/{fileId}/lock -> /v2/accounts/{accountUid}/files/{fileUid}/lock
  * /files/{fileId}/unlock -> /v2/accounts/{accountUid}/files/{fileUid}/unlock
  * /files/{fileId}/related -> /v2/accounts/{accountUid}/files/{fileUid}/related
  * /files/{fileId}/restore -> /v2/accounts/{accountUid}/files/{fileUid}/restore
  * /files/{fileId}/purge -> /v2/accounts/{accountUid}/files/{fileUid}/purge
  * /files/{fileId}/versions -> /v2/accounts/{accountUid}/files/{fileUid}/versions
  * /files/{fileId}/versions/{fileVersionId} -> /v2/accounts/{accountUid}/files/{fileUid}/versions/{fileVersionUid}
  * /files/download/{fileId} -> /v2/accounts/{accountUid}/files/{fileUid}/download
  * /files/versions/{fileVersionId}/preview -> /v2/accounts/{accountUid}/files/{fileUid}/versions/{fileVersionUid}/preview
  * /files/{fileId}/unpack -> /v2/accounts/{accountUid}/files/{fileUid}/unpack
  * /files/unpack-status/{key} -> /v2/accounts/{accountUid}/unpack/status/{key}
  * /files/unpack-check -> /v2/accounts/{accountUid}/unpack/check
  * /files/languages -> /v2/accounts/{accountUid}/document-languages
  * /folders/single/{folderId} -> /v2/accounts/{accountUid}/folders/{folderUid}
  * /files/people/{personId} -> /v2/accounts/{accountUid}/people/{personUid}
  * /files/people/{personId}/permissions -> /v2/accounts/{accountUid}/people/{personUid}/permissions
  * /folders/{folderId}/files -> /v2/accounts/{accountUid}/folders/{folderUid}/files
  * /folders/single/{folderId} -> /v2/accounts/{accountUid}/folders/{folderUid}
  * /folders/{folderId}/permissions -> /v2/accounts/{accountUid}/folders/{folderUid}/permissions
  * /folders/{folderId}/folders -> /v2/accounts/{accountUid}/folders/{folderUid}/folders
  * /folders/{folderId}/restore -> /v2/accounts/{accountUid}/folders/{folderUid}/restore
  * /folders/{folderId}/purge -> /v2/accounts/{accountUid}/folders/{folderUid}/purge
  * /groups/{groupId} -> /v2/accounts/{accountUid}/groups/{groupUid}
  * /groups/{groupId}/permissions -> /v2/accounts/{accountUid}/groups/{groupUid}/permissions
  * /groups/{groupId}/shares -> /v2/accounts/{accountUid}/groups/{groupUid}/shares
  * /groups/{groupId}/users -> /v2/accounts/{accountUid}/groups/{groupUid}/users
  * /groups/{groupId}/users/{userId} -> /v2/accounts/{accountUid}/groups/{groupUid}/users/{userUid}
  * /groups/{groupId}/users/{userId}/permissions -> /v2/accounts/{accountUid}/groups/{groupUid}/users/{userUid}/permissions
  * /meta/{metaId}/permissions -> /v2/accounts/{accountUid}/meta/{metaUid}/permissions
  * /files/{fileId}/meta -> /v2/accounts/{accountUid}/files/{fileUid}/meta
  * /files/{fileId}/meta/{metaId} -> /v2/accounts/{accountUid}/files/{fileUid}/meta/{metaUid}
  * /files/{fileId}/meta/active -> /v2/accounts/{accountUid}/files/{fileUid}/active-meta
  * /folders/{folderId}/meta -> /v2/accounts/{accountUid}/folders/{folderUid}/meta
  * /folders/{folderId}/meta/{metaId} -> /v2/accounts/{accountUid}/folders/{folderUid}/meta/{metaUid}
  * /folders/{folderId}/meta/active -> /v2/accounts/{accountUid}/folders/{folderUid}/active-meta
  * /files/people/{personId} -> /v2/accounts/{accountUid}/people/{personUid}
  * /files/people/{personId}/permissions -> /v2/accounts/{accountUid}/people/{personUid}/permissions
  * /files/{fileId}/people -> /v2/accounts/{accountUid}/files/{fileUid}/people
  * /files/{fileId}/people/{personId} -> /v2/accounts/{accountUid}/files/{fileUid}/people/{personUid}
  * /files/reminders/{reminderId} -> /v2/accounts/{accountUid}/reminders/{reminderUid}
  * /files/{fileId}/reminders -> /v2/accounts/{accountUid}/files/{fileUid}/reminders
  * /search/extensions -> /v2/accounts/{accountUid}/search/extensions
  * /search/files -> /v2/accounts/{accountUid}/search/files
  * /search/folders -> /v2/accounts/{accountUid}/search/folders
  * /search/simple -> /v2/accounts/{accountUid}/search/simple
  * /shares/{shareId} -> /v2/accounts/{accountUid}/shares/{shareUid}
  * /shares/{shareId}/permissions -> /v2/accounts/{accountUid}/shares/{shareUid}/permissions
  * /files/{fileId}/shares -> /v2/accounts/{accountUid}/files/{fileUid}/shares
  * /folders/{folderId}/shares -> /v2/accounts/{accountUid}/folders/{folderUid}/shares
  * /files/tags/{tagId} -> /v2/accounts/{accountUid}/tags/{tagUid}
  * /files/tags/{tagId}/permissions -> /v2/accounts/{accountUid}/tags/{tagUid}/permissions
  * /files/{fileId}/tags -> /v2/accounts/{accountUid}/files/{fileUid}/tags
  * /files/{fileId}/tags/{tagId} -> /v2/accounts/{accountUid}/files/{fileUid}/tags/{tagUid}
  * /audit/accountLog/{accountId} -> /v2/accounts/{accountUid}/audit/accountLog
  * /files/{fileId}/audit -> /v2/accounts/{accountUid}/files/{fileUid}/audit
  * /folders/{folderId}/audit -> /v2/accounts/{accountUid}/folders/{folderUid}/audit


## Authentication
This API uses OAuth v2 Bearer Token / Personal Access Token for its authentication.

# Group User

User-related operations

## V2 User [/v2/user{?fields,expand}]

### getUser [GET]
Returns user details
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include additional information (comma-separated).

        + Sample: ["dateFormat"]
        + Members
            + `dateFormat` 
            + `timeFormat` 


+ Response 200 (application/json)

        User information

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### editUser [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include additional information (comma-separated).

        + Sample: ["dateFormat"]
        + Members
            + `dateFormat` 
            + `timeFormat` 


+ Request (<<Add Header Value>>)

    + Attributes (V2 User Request)



+ Response 200 (application/json)

        User information

    + Attributes (string)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 User Billing [/v2/user/billing{?fields}]

### editUserBilling [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)

    + Attributes (string)



+ Response 200 (application/json)

        User profile

    + Attributes (string)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 User Countries [/v2/user/countries]

### getSupportedCountries [GET]

+ Response 200 (application/json)

        Supported countries

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 User Countries By Locale [/v2/user/countries/{locale}]

+ Parameters
    + locale (string, required)

        Locale

        + Sample: en-GB

### getSupportCountriesInLocale [GET]

+ Response 200 (application/json)

        Supported countries

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 User Locales [/v2/user/locales]

### getSupportedLocales [GET]

+ Response 200 (application/json)

        Supported locales

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 User Permissions [/v2/user/permissions]

### getUserPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (V2 User Permissions Response)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Users [/v2/users{?fields}]

### signupUser [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (newUserSignup)



+ Response 200 (application/json)

        User signed up

    + Attributes (Other user)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Users By UserUid [/v2/users/{userUid}{?fields}]

+ Parameters
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### getUserByUid [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        User information

    + Attributes (Other user)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Users Shares By AccountUid [/v2/accounts/{accountUid}/users/{userUid}/shares{?fields,per-page,cursor,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### getUserShares [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + cursor (string, optional)

        Pagination cursor  
        The `Link` header will have a next page link available with the new cursor value, usually contains the last `uid` value from the response.

    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Item shares

    + Attributes (array[string])



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Users Access By AccountUid [/v2/accounts/{accountUid}/users/{userUid}/access{?fields,per-page,cursor,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### getAllUserShares [GET]
All shares (including through groups) accessible by an user
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + cursor (string, optional)

        Pagination cursor  
        The `Link` header will have a next page link available with the new cursor value, usually contains the last `uid` value from the response.

    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Item shares

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Account

Account-related operations

## V2 Accounts Team Users By AccountUid [/v2/accounts/{accountUid}/team-users{?fields,expand,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getTeamUsers [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include additional information (comma-separated).

        + Sample: ["remoteUsername"]
        + Members
            + `remoteUsername` 
            + `status` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 100
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * email
          * name


+ Response 200 (application/json)

        Array of team users

    + Attributes (array[string])



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Team Users By AccountUid [/v2/accounts/{accountUid}/team-users{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### addTeamUser [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include additional information (comma-separated).

        + Sample: ["remoteUsername"]
        + Members
            + `remoteUsername` 
            + `status` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        User signed up / added

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Team Users By AccountUid [/v2/accounts/{accountUid}/team-users/{userUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### getTeamUser [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include additional information (comma-separated).

        + Sample: ["remoteUsername"]
        + Members
            + `remoteUsername` 
            + `status` 


+ Response 200 (application/json)

        Team user

    + Attributes (string)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts [/v2/accounts{?fields,expand}]

### getAccounts [GET]
Returns available accounts
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["shares"]
        + Members
            + `shares` 
            + `stats` 


+ Response 200 (application/json)

        Array of accounts

    + Attributes (array[string])



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### createAccount [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["shares"]
        + Members
            + `shares` 
            + `stats` 


+ Request (<<Add Header Value>>)

    + Attributes (V2 Accounts Request)



+ Response 200 (application/json)

        Account created

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts By AccountUid [/v2/accounts/{accountUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccount [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["shares"]
        + Members
            + `shares` 
            + `stats` 


+ Response 200 (application/json)

        Account info

    + Attributes (string)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateAccount [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["shares"]
        + Members
            + `shares` 
            + `stats` 


+ Request (<<Add Header Value>>)

    + Attributes (V2 Accounts Request1)



+ Response 200 (application/json)

        Account info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteAccount [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["shares"]
        + Members
            + `shares` 
            + `stats` 


+ Response 200 (application/json)

        Account info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Permissions By AccountUid [/v2/accounts/{accountUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccountPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (permissions)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Users By AccountUid [/v2/accounts/{accountUid}/users{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccountUsers [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Array of Account Users

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Users By AccountUid [/v2/accounts/{accountUid}/users/{userUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### getAccountUser [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Account User

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateAccountUser [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Users Request)



+ Response 200 (application/json)

        Account User

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteAccountUser [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Account User

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Section Order By AccountUid [/v2/accounts/{accountUid}/section-order]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getSectionOrder [GET]

+ Response 200 (application/json)

        Account section order, if configured

    + Attributes (array[string])


    + Body

            [
              "asd",
              "asdf",
              "asdg"
            ]


+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateSectionOrder [PUT]
Unlisted sections will be ordered by name after listed sections

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (array[string])



+ Response 200 (application/json)

        Account section order

    + Attributes (array[number])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Download By AccountUid [/v2/accounts/{accountUid}/download]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### downloadFilesAndFoldersBundle [GET]
To download whole account, omit files and folders properties  
Alternatively, POST request is also supported

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/octet-stream)

        ZIP Archive




+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Entity

Entity-related operations

## V2 Accounts Entities By AccountUid [/v2/accounts/{accountUid}/entities{?fields,expand,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getEntities [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * name
          * createdAt
          * updatedAt
          * type
          * retentionEnd


+ Response 200 (application/json)

        Section list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities By AccountUid [/v2/accounts/{accountUid}/entities/{entityUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntity [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Response 200 (application/json)

        Entity info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateEntity [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Request)



+ Response 200 (application/json)

        Updated entity

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteEntity [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        Deleted entity

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Entities [/v2/accounts/{accountUid}/entities/{entityUid}/entities{?page,per-page,fields,expand,sort,sort-folders-first,extension}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntitiesByParent [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]
    + sort (array[string], optional)

        Comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * name
          * date
          * due_date
          * size
          * mail_sender
          * retention_end
          * language
          * number
          * created_at
          * updated_at
          * recycle_date
          * creator
          * extension
          * meta:{uid}

        + Sample: ["date","name","-created_at"]
    + sort-folders-first (string, optional)

        Set folders to be sorted first in results

        + Sample: true
    + extension (string, optional)

        Filter by extension

        + Sample: png

+ Response 200 (application/json)

        Entity list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities All By AccountUid [/v2/accounts/{accountUid}/entities/all{?per-page,cursor,fields,expand,folders,files,parentUid,extension}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAllEntities [GET]
+ Parameters
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + cursor (string, optional)

        Pagination cursor  
        The `Link` header will have a next page link available with the new cursor value, usually contains the last `uid` value from the response.

    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]
    + folders (string, optional)

        Include folders. If only specifying one, the other is opposite value

        + Sample: 1
    + files (string, optional)

        Include files. If only specifying one, the other is opposite value

        + Sample: 1
    + parentUid (string, optional)

        Limit response to entities below specific parent

        + Sample: Abc_123-XY
    + extension (string, optional)

        Filter by extension

        + Sample: png

+ Response 200 (application/json)

        Entity list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Parents [/v2/accounts/{accountUid}/entities/{entityUid}/parents{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityParents [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Response 200 (application/json)

        Parent entities list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Related [/v2/accounts/{accountUid}/entities/{entityUid}/related{?fields,expand,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityRelatedItems [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * createdAt
          * entityUid


+ Request 
    + Headers

            X-Pagination:<<Add Header Value>>





+ Response 200 (application/json)

        Array of relations  
When `X-Pagination` header, `page`, `page-size` or `sort` parameter is present, the response will be paginated and include pagination-related headers.

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Related [/v2/accounts/{accountUid}/entities/{entityUid}/related{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### addEntityRelatedItem [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Related Request)



+ Response 200 (application/json)

        Related item

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteEntityRelatedItem [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Related Request)



+ Response 200 (application/json)

        Related item

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Restore [/v2/accounts/{accountUid}/entities/{entityUid}/restore{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### restoreEntity [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Restore Request)



+ Response 200 (application/json)

        Folder info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Purge By AccountUid [/v2/accounts/{accountUid}/entities/{entityUid}/purge{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### deleteEntityPermanently [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Purged entity info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Action Duplicate [/v2/accounts/{accountUid}/entities/action/duplicate]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### duplicateEntities [POST]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (duplicate)



+ Response 200 (application/json)

        Duplication status

    + Attributes (duplicationStatus)



+ Response 302 (application/json)

        Duplicate status URL

    + Attributes (V2 Accounts Entities Action Duplicate Response)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Action Duplicate [/v2/accounts/{accountUid}/entities/action/duplicate/{statusKey}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + statusKey (string, required)

        Duplication status key

        + Sample: Abc_123-

### getDuplicationStatus [GET]

+ Response 200 (application/json)

        Duplication status data

    + Attributes (duplicationStatus)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Recycle Bin By AccountUid [/v2/accounts/{accountUid}/recycle-bin{?page,per-page,fields,expand,parentUid}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getRecycledEntities [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]
    + parentUid (string, optional)

        Limit response to entities below specific parent

        + Sample: Abc_123-XY

+ Response 200 (application/json)

        Recycled entity entries

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Recycle Bin By AccountUid [/v2/accounts/{accountUid}/recycle-bin{?page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### emptyRecycleBin [DELETE]
Purge all entities from the recycle bin
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1

+ Response 204 

        All entities purged




+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 500 (application/json)

        Internal Server Error

    + Attributes (500serverError1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Shared Entities [/v2/accounts/{accountUid}/shared-entities{?page,per-page,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getSharedEntities [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]

+ Response 200 (application/json)

        Shared entities

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags Entities By AccountUid [/v2/accounts/{accountUid}/tags/{tagUid}/entities{?per-page,cursor,fields,expand,extension}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### getEntitiesByTag [GET]
+ Parameters
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + cursor (string, optional)

        Pagination cursor  
        The `Link` header will have a next page link available with the new cursor value, usually contains the last `uid` value from the response.

    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.people,file.version). sectionOrder is applicable to only root entities/sections.  
        meta can be specified with an UID identifier to only include that specific meta like this: `meta:abcde12345`. Fields are to be specified without UID identifier.

        + Sample: ["tags","extension","meta:abcde12345","file.version"]
    + extension (string, optional)

        Filter by extension

        + Sample: png

+ Response 200 (application/json)

        Entity list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Folder

Folder-related operations

## V2 Accounts Folders Files By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}/files{?page,per-page,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFilesByParent [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Response 200 (application/json)

        File list

    + Attributes (array[string])



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Reports File List [/v2/accounts/{accountUid}/reports/file-list{?fields,limit,last}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccountFileReport [GET]
Use [`GET /v2/accounts/{accountUid}/entities/all`](#tag/Entity/operation/getAllEntities) instead
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + limit (number, optional) -

        File count limit

        + Default: 10000
        + Sample: 1
    + last (string, optional)

        Start output after this UID

        + Sample: Abc_123-XY

+ Response 200 (application/json)

        File list

    + Attributes (array[File3])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders By AccountUid [/v2/accounts/{accountUid}/folders{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getFolders [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Response 200 (application/json)

        Section list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### createFolder [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        Created section/folder

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolder [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Response 200 (application/json)

        Folder info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateFolder [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Request)



+ Response 200 (application/json)

        Updated folder

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFolder [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Deleted folder

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Email By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}/email]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### configureFolderEmail [POST]
Request body is optional, action defaults to `activate`

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        Mail-in address info

    + Attributes (Mailin address)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFolderEmail [DELETE]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Mail-in address info

    + Attributes (Mailin address)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### requestNewFolderEmailAddress [PUT]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Mail-in address info

    + Attributes (Mailin address)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Permissions [/v2/accounts/{accountUid}/folders/{folderUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (folderpropertiespermissions)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Folders [/v2/accounts/{accountUid}/folders/{folderUid}/folders{?page,per-page,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderByParent [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Response 200 (application/json)

        Folder list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Related [/v2/accounts/{accountUid}/folders/{folderUid}/related{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderRelatedItems [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Array of relations

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addFolderRelatedItem [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Related Request)



+ Response 200 (application/json)

        Related item

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFolderRelatedItem [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Related Request)



+ Response 200 (application/json)

        Related item

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Restore [/v2/accounts/{accountUid}/folders/{folderUid}/restore{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### restoreFolder [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Restore Request)



+ Response 200 (application/json)

        Folder info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Purge By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}/purge{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### deleteFolderPermanently [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Purged folder info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Recycle Bin Folders [/v2/accounts/{accountUid}/recycle-bin/folders{?page,per-page,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getRecycledFolders [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.shares). sectionOrder is applicable to only sections.

        + Sample: ["parent","meta"]
        + Members
            + `parent` 
            + `meta` 
            + `shares` 
            + `extra` 
            + `sectionOrder` 
            + `inboxFolder` 
            + `teamFolder` 
            + `folderStats` 
            + `sort` 
            + `viewType` 


+ Response 200 (application/json)

        Recycle bin folder entries

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Shared Items By AccountUid [/v2/accounts/{accountUid}/shared-items{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getSharedFilesAndFolders [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 


+ Response 200 (application/json)

        Shared files and folders

    + Attributes (V2 Accounts Shared Items Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group File

File-related operators

## V2 Accounts Files Upload By AccountUid [/v2/accounts/{accountUid}/files/upload]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### uploadFile [POST]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        Uploader response

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFile [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateFile [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Files Request)



+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFile [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Permissions [/v2/accounts/{accountUid}/files/{fileUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFilePermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (propertiespermissions)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Lock By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/lock{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### lockFilePost [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Files Lock Request)



+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### lockFilePut [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Files Lock Request)



+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### unlockFile [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Unlock By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/unlock{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### unlockFile2 [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Files Unlock Request)



+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Related By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/related{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileRelatedItems [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Array of relations

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addFileRelatedItem [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        Related item

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFileRelatedItem [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        Related item

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Restore By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/restore{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### restoreFile [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Files Restore Request)



+ Response 200 (application/json)

        File info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Purge By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/purge{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### deleteFilePermanently [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Purged file info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Versions By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/versions{?fields,expand,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileVersions [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * createdAt
          * uid
          * size


+ Request 
    + Headers

            X-Pagination:<<Add Header Value>>





+ Response 200 (application/json)

        Array of fileVersions.  
When `X-Pagination` header, `page`, `page-size` or `sort` parameter is present, the response will be paginated and include pagination-related headers.

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Versions FileVersionUid [/v2/accounts/{accountUid}/files/{fileUid}/versions/{fileVersionUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + fileVersionUid (string, required)

        File Version UID

        + Sample: Abc_123-XY

### deleteFileVersion [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        File Version info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Download By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/download]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileDownloadUrl [GET]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 302 (application/json)

        Link to file download

    + Attributes (V2 Accounts Files Download Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Versions FileVersionUid Download [/v2/accounts/{accountUid}/files/{fileUid}/versions/{fileVersionUid}/download]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + fileVersionUid (string, required)

        File Version UID

        + Sample: Abc_123-XY

### getFileVersionDownloadUrl [GET]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 302 (application/json)

        Link to file version download

    + Attributes (V2 Accounts Files Versions FileVersionUid Download Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Preview By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/preview]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFilePreviewDownloadUrl [GET]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 302 (application/json)

        Link to file preview

    + Attributes (V2 Accounts Files Preview Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Versions FileVersionUid Preview [/v2/accounts/{accountUid}/files/{fileUid}/versions/{fileVersionUid}/preview]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + fileVersionUid (string, required)

        File Version UID

        + Sample: Abc_123-XY

### getFileVersionPreviewDownloadUrl [GET]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 302 (application/json)

        Link to file preview

    + Attributes (V2 Accounts Files Versions FileVersionUid Preview Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Recycle Bin Files [/v2/accounts/{accountUid}/recycle-bin/files{?page,per-page,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getRecycledFiles [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example parent.parent.parent)

        + Members
            + `parent` 
            + `meta` 
            + `people` 
            + `tags` 
            + `reminders` 
            + `shares` 
            + `fileRelations` 
            + `version` 


+ Response 200 (application/json)

        Recycle bin file entries

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Unpack By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/unpack]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### unpackArchiveFile [POST]
After initiating unpacking, check status with received key

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Files Unpack Request)



+ Response 200 (application/json)

        Unpack key, check status from [`GET /accounts/{accountUid}/files/unpack-status/{key}`](#tag/File/operation/getUnpackStatus)

    + Attributes (V2 Accounts Files Unpack Response)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Unpack Status By AccountUid [/v2/accounts/{accountUid}/unpack/status/{key}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + key (string, required)

        Unpack key


### getUnpackStatus [GET]

+ Response 200 (application/json)

        Unpacking status

    + Attributes (string)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Unpack Check By AccountUid [/v2/accounts/{accountUid}/unpack/check]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### checkUnpackSupport [POST]
Send a filename to check if system supports unpacking it

+ Request (<<Add Header Value>>)

    + Attributes (V2 Accounts Unpack Check Request)



+ Response 200 (application/json)

        Unpacking support response

    + Attributes (boolean)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Document Languages [/v2/accounts/{accountUid}/document-languages]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getDocumentOcrLanguages [GET]

+ Response 200 (application/json)

        Supported languages

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Meta

Meta-related operations

## V2 Accounts Meta By AccountUid [/v2/accounts/{accountUid}/meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getMetas [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of Meta info

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addMeta [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Meta Request)



+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Meta By AccountUid [/v2/accounts/{accountUid}/meta/{metaUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY

### getMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateMeta [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Meta Request1)



+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteMeta [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Meta Permissions [/v2/accounts/{accountUid}/meta/{metaUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY

### getMetaPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (metaBasepropertiespermissions)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Meta Options By AccountUid [/v2/accounts/{accountUid}/meta/{metaUid}/options{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY

### addMetaOptions [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Meta Options Request)



+ Response 200 (application/json)

        Meta

    + Attributes (Meta Select Option)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Meta Options OptionUid [/v2/accounts/{accountUid}/meta/{metaUid}/options/{optionUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY
    + optionUid (string, required)

        Meta Select Option UID

        + Sample: Abc_123-XY

### updateMetaOption [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Meta Options OptionUid Request)



+ Response 200 (application/json)

        Meta

    + Attributes (Meta Select Option)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteMetaOption [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Meta Select Option

    + Attributes (Meta Select Option)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Meta By AccountUid [/v2/accounts/{accountUid}/entities/{entityUid}/meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of entityMeta

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addEntityMeta [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Meta Request)



+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Meta MetaUid [/v2/accounts/{accountUid}/entities/{entityUid}/meta/{metaUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY

### editEntityMeta [PUT]
In case of having multiple file metas referencing the same meta, the selection of affected entity meta is undetermined, use [`PUT /accounts/{accountUid}/entities/{entityUid}/meta/{metaUid}/{entityMetaUid}`](#tag/Entity/operation/editSpecificEntityMeta) instead.
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Meta MetaUid Request)


    + Body

            {
                "value": null
            }


+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteEntityMeta [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Meta MetaUid EntityMetaUid [/v2/accounts/{accountUid}/entities/{entityUid}/meta/{metaUid}/{entityMetaUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY
    + entityMetaUid (string, required)

        Entity Meta UID

        + Sample: Abc_123-XY

### editSpecificEntityMeta [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Meta MetaUid EntityMetaUid Request)


    + Body

            {
                "value": null
            }


+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteSpecificEntityMeta [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Active Meta [/v2/accounts/{accountUid}/entities/{entityUid}/active-meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getActiveEntityMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of active folderMeta

    + Attributes (array[Folder meta])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Meta By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of fileMeta

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addFileMeta [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Files Meta Request)



+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Meta MetaUid [/v2/accounts/{accountUid}/files/{fileUid}/meta/{metaUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY

### updateFileMeta [PUT]
In case of having multiple file metas referencing the same meta, the selection of affected file meta is undetermined, use [`PUT /accounts/{accountUid}files/{fileUid}/meta/{metaUid}/{fileMetaUid}`](#tag/Meta/operation/updateSpecificFileMeta) instead.
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Files Meta MetaUid Request)


    + Body

            {
                "value": null
            }


+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFileMeta [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Meta MetaUid FileMetaUid [/v2/accounts/{accountUid}/files/{fileUid}/meta/{metaUid}/{fileMetaUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY
    + fileMetaUid (string, required)

        File Meta UID

        + Sample: Abc_123-XY

### updateSpecificFileMeta [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Files Meta MetaUid FileMetaUid Request)


    + Body

            {
                "value": null
            }


+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteSpecificFileMeta [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        Meta

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Active Meta [/v2/accounts/{accountUid}/files/{fileUid}/active-meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileActiveMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of active folderMeta

    + Attributes (array[Folder meta])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Meta By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}/meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of folderMeta

    + Attributes (array[Folder meta])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addFolderMeta [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Meta Request)



+ Response 200 (application/json)

        Folder Meta

    + Attributes (Folder meta)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Meta MetaUid [/v2/accounts/{accountUid}/folders/{folderUid}/meta/{metaUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY
    + metaUid (string, required)

        Meta UID

        + Sample: Abc_123-XY

### updateFolderMeta [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Meta MetaUid Request)



+ Response 200 (application/json)

        Folder Meta

    + Attributes (Folder meta)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteFolderMeta [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Folder Meta

    + Attributes (Folder meta)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Active Meta [/v2/accounts/{accountUid}/folders/{folderUid}/active-meta{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderActiveMeta [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of active folderMeta

    + Attributes (array[Folder meta])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Tag

Tag-related operations

## V2 Accounts Tags By AccountUid [/v2/accounts/{accountUid}/tags{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getTags [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * name
          * createdAt
          * updatedAt

        + Sample: ["name","-uid"]

+ Response 200 (application/json)

        Tag list

    + Attributes (array[Tag])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags By AccountUid [/v2/accounts/{accountUid}/tags{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### addTag [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Tags Request)



+ Response 200 (application/json)

        Added Tag info

    + Attributes (Tag)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags Root By AccountUid [/v2/accounts/{accountUid}/tags/root{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getRootTags [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * name
          * createdAt
          * updatedAt

        + Sample: ["name","-uid"]

+ Response 200 (application/json)

        Tag list

    + Attributes (array[Tag])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags By AccountUid [/v2/accounts/{accountUid}/tags/{tagUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### getTag [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Tag info

    + Attributes (Tag)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateTag [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Tags Request1)



+ Response 200 (application/json)

        Tag info

    + Attributes (Tag)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteTag [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Tag info

    + Attributes (Tag)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags Permissions [/v2/accounts/{accountUid}/tags/{tagUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### getTagPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (tagpropertiespermissions)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags Tags By AccountUid [/v2/accounts/{accountUid}/tags/{tagUid}/tags{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### getSubTags [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * name
          * createdAt
          * updatedAt

        + Sample: ["name","-uid"]

+ Response 200 (application/json)

        Tag list

    + Attributes (array[Tag])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Tags By AccountUid [/v2/accounts/{accountUid}/entities/{entityUid}/tags{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityTags [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of tags

    + Attributes (array[Tag])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addEntityTags [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Entities Tags Request)



+ Response 200 (application/json)

        Added tags

    + Attributes (array[Tag])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Tags TagUid [/v2/accounts/{accountUid}/entities/{entityUid}/tags/{tagUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### deleteEntityTag [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        Tag info

    + Attributes (Tag)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Tags By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/tags{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileTags [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of tags

    + Attributes (array[Tag])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addFileTags [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Files Tags Request)



+ Response 200 (application/json)

        Added tags

    + Attributes (array[Tag])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Tags TagUid [/v2/accounts/{accountUid}/files/{fileUid}/tags/{tagUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### deleteFileTags [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        Tag info

    + Attributes (Tag)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Person

Person-related operations

## V2 Accounts People By AccountUid [/v2/accounts/{accountUid}/people{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getPeople [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of Person info

    + Attributes (array[Person])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addPerson [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts People Request)



+ Response 200 (application/json)

        Added Person info

    + Attributes (Person)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts People By AccountUid [/v2/accounts/{accountUid}/people/{personUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + personUid (string, required)

        Person UID

        + Sample: Abc_123-XY

### getPerson [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Person info

    + Attributes (Person)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updatePerson [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts People Request)



+ Response 200 (application/json)

        Person info

    + Attributes (Person)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deletePerson [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Person info

    + Attributes (Person)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts People Permissions [/v2/accounts/{accountUid}/people/{personUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + personUid (string, required)

        Person UID

        + Sample: Abc_123-XY

### getPersonPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (personpropertiespermissions)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files People By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/people{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFilePeople [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of people

    + Attributes (array[Person])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### addFilePeople [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>


    + Attributes (V2 Accounts Files People Request)



+ Response 200 (application/json)

        Added people

    + Attributes (array[Person])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files People PersonUid [/v2/accounts/{accountUid}/files/{fileUid}/people/{personUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY
    + personUid (string, required)

        Person UID

        + Sample: Abc_123-XY

### deleteFilePerson [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>
            X-Folderit-Lock:<<Add Header Value>>





+ Response 200 (application/json)

        Person info

    + Attributes (Person)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Reminder

Reminder-related operations

## V2 Accounts Reminders By AccountUid [/v2/accounts/{accountUid}/reminders{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAllReminders [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * entityUid
          * createdAt
          * remindAt
          * email
          * groupUid


+ Request 
    + Headers

            X-Pagination:<<Add Header Value>>





+ Response 200 (application/json)

        Array of reminders  
When `X-Pagination` header, `page`, `page-size` or `sort` parameter is present, the response will be paginated and include pagination-related headers.

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Reminders By AccountUid [/v2/accounts/{accountUid}/reminders/{reminderUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + reminderUid (string, required)

        Reminder UID

        + Sample: Abc_123-XY

### updateReminder [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Reminders Request)



+ Response 200 (application/json)

        Reminder info

    + Attributes (reminder)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteReminder [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Reminder info

    + Attributes (reminder)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Reminders [/v2/accounts/{accountUid}/files/{fileUid}/reminders{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileReminders [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * entityUid
          * createdAt
          * remindAt
          * email
          * groupUid


+ Request 
    + Headers

            X-Pagination:<<Add Header Value>>





+ Response 200 (application/json)

        Array of reminders  
When `X-Pagination` header, `page`, `page-size` or `sort` parameter is present, the response will be paginated and include pagination-related headers.

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Reminders [/v2/accounts/{accountUid}/files/{fileUid}/reminders{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### addFileReminders [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (array[AddRemindersRequest])



+ Response 200 (application/json)

        Added reminders

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Reminders [/v2/accounts/{accountUid}/folders/{folderUid}/reminders{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderReminders [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * entityUid
          * createdAt
          * remindAt
          * email
          * groupUid


+ Request 
    + Headers

            X-Pagination:<<Add Header Value>>





+ Response 200 (application/json)

        Array of reminders  
When `X-Pagination` header, `page`, `page-size` or `sort` parameter is present, the response will be paginated and include pagination-related headers.

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Reminders [/v2/accounts/{accountUid}/folders/{folderUid}/reminders{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### addFolderReminders [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (array[AddRemindersRequest])



+ Response 200 (application/json)

        Added reminders

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Reminders [/v2/accounts/{accountUid}/entities/{entityUid}/reminders{?fields,page,per-page,sort}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityReminders [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 20
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order
        One of:
          * uid
          * entityUid
          * createdAt
          * remindAt
          * email
          * groupUid


+ Request 
    + Headers

            X-Pagination:<<Add Header Value>>





+ Response 200 (application/json)

        Array of reminders  
When `X-Pagination` header, `page`, `page-size` or `sort` parameter is present, the response will be paginated and include pagination-related headers.

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Reminders [/v2/accounts/{accountUid}/entities/{entityUid}/reminders{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### addEntityReminders [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (array[AddRemindersRequest])



+ Response 200 (application/json)

        Added reminders

    + Attributes (array[reminder])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Resolution

Resolution-related operations

## V2 Accounts Entities Resolutions [/v2/accounts/{accountUid}/entities/{entityUid}/resolutions{?fields,expand,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityResolutions [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Resolution list

    + Attributes (array[string])



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Resolutions [/v2/accounts/{accountUid}/entities/{entityUid}/resolutions{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### addEntityResolution [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (resolutioncreate)



+ Response 201 (application/json)

        Added resolution

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 409 (application/json)

        Conflict

    + Attributes (409conflict1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Resolutions By AccountUid [/v2/accounts/{accountUid}/resolutions/{resolutionUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + resolutionUid (string, required)

        Resolution UID

        + Sample: Abc_123-XY

### getResolution [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Resolution

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateResolution [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (resolutionupdate)



+ Response 200 (application/json)

        Updated resolution

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Resolutions By AccountUid [/v2/accounts/{accountUid}/resolutions/{resolutionUid}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + resolutionUid (string, required)

        Resolution UID

        + Sample: Abc_123-XY

### deleteResolution [DELETE]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 204 

        No content




+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Resolutions Invites [/v2/accounts/{accountUid}/resolutions/{resolutionUid}/invites{?fields,expand,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + resolutionUid (string, required)

        Resolution UID

        + Sample: Abc_123-XY

### getResolutionInvites [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Resolution invite list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Resolutions Invites [/v2/accounts/{accountUid}/resolutions/{resolutionUid}/invites{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + resolutionUid (string, required)

        Resolution UID

        + Sample: Abc_123-XY

### addResolutionInvites [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (array[resolutioninvite])



+ Response 201 (application/json)

        Added resolution invites

    + Attributes (array[string])



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Resolutions Invites ResolutionInviteUid [/v2/accounts/{accountUid}/resolutions/{resolutionUid}/invites/{resolutionInviteUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + resolutionUid (string, required)

        Resolution UID

        + Sample: Abc_123-XY
    + resolutionInviteUid (string, required)

        Resolution invite UID

        + Sample: Abc_123-XY

### getResolutionInvite [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Resolution

    + Attributes (string)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateResolutionInvite [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (inviteupdate)



+ Response 200 (application/json)

        Updated resolution

    + Attributes (string)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Resolutions Invites ResolutionInviteUid [/v2/accounts/{accountUid}/resolutions/{resolutionUid}/invites/{resolutionInviteUid}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + resolutionUid (string, required)

        Resolution UID

        + Sample: Abc_123-XY
    + resolutionInviteUid (string, required)

        Resolution invite UID

        + Sample: Abc_123-XY

### deleteResolutionInvite [DELETE]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 204 

        No content




+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Workflow

Workflow-related operations

## V2 Accounts Entities Workflows [/v2/accounts/{accountUid}/entities/{entityUid}/workflows{?fields,expand,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### getEntityWorkflows [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Workflow list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Entities Workflows [/v2/accounts/{accountUid}/entities/{entityUid}/workflows{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + entityUid (string, required)

        Entity UID

        + Sample: Abc_123-XY

### addEntityWorkflow [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowcreate)



+ Response 201 (application/json)

        Added workflow

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows By AccountUid [/v2/accounts/{accountUid}/workflows{?fields,expand,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccountWorkflows [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Workflow list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows By AccountUid [/v2/accounts/{accountUid}/workflows{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### addWorkflow [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowcreate)



+ Response 201 (application/json)

        Added workflow

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows All By AccountUid [/v2/accounts/{accountUid}/workflows/all{?fields,expand,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAllWorkflows [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Workflow list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows By AccountUid [/v2/accounts/{accountUid}/workflows/{workflowUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY

### getWorkflow [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Workflow

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateWorkflow [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowupdate)



+ Response 200 (application/json)

        Updated workflow

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows By AccountUid [/v2/accounts/{accountUid}/workflows/{workflowUid}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY

### deleteWorkflow [DELETE]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 204 

        No content




+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Versions [/v2/accounts/{accountUid}/workflows/{workflowUid}/versions{?fields,expand,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY

### getWorkflowVersions [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 

    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Workflow version list

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Versions [/v2/accounts/{accountUid}/workflows/{workflowUid}/versions{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY

### addWorkflowVersion [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowversioncreate)



+ Response 201 (application/json)

        Added workflow

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Versions WorkflowVersionId [/v2/accounts/{accountUid}/workflows/{workflowUid}/versions/{workflowVersionId}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY
    + workflowVersionId (number, required)

        Workflow version ID

        + Sample: 1

### getWorkflowVersion [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Response 200 (application/json)

        Workflow version

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateWorkflowVersion [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated) from the expanded variant of response.

        + Members
            + `property1` 
            + `property2` 
            + `property3` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowversionupdate)



+ Response 200 (application/json)

        Updated workflow version

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Versions WorkflowVersionId [/v2/accounts/{accountUid}/workflows/{workflowUid}/versions/{workflowVersionId}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY
    + workflowVersionId (number, required)

        Workflow version ID

        + Sample: 1

### deleteWorkflowVersion [DELETE]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 204 

        No content




+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Triggers [/v2/accounts/{accountUid}/workflows/{workflowUid}/triggers{?fields,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY

### getWorkflowTriggers [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Workflow trigger list

    + Attributes (array[Trigger])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Triggers [/v2/accounts/{accountUid}/workflows/{workflowUid}/triggers{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY

### addWorkflowTrigger [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowtriggercreate)



+ Response 201 (application/json)

        Added workflow

    + Attributes (Trigger)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Triggers WorkflowTriggerId [/v2/accounts/{accountUid}/workflows/{workflowUid}/triggers/{workflowTriggerId}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY
    + workflowTriggerId (number, required)

        Workflow trigger ID

        + Sample: 1

### getWorkflowTrigger [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Workflow trigger

    + Attributes (Trigger)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateWorkflowTrigger [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (workflowtriggerupdate)



+ Response 200 (application/json)

        Updated workflow version

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Workflows Triggers WorkflowTriggerId [/v2/accounts/{accountUid}/workflows/{workflowUid}/triggers/{workflowTriggerId}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + workflowUid (string, required)

        Workflow UID

        + Sample: Abc_123-XY
    + workflowTriggerId (number, required)

        Workflow trigger ID

        + Sample: 1

### deleteWorkflowTrigger [DELETE]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 204 

        No content




+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Share

Share-related operations

## V2 Accounts Access Users By AccountUid [/v2/accounts/{accountUid}/access/users{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAllUsersWithAccess [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Array of other users

    + Attributes (array[Other user])



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Shares By AccountUid [/v2/accounts/{accountUid}/shares{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### listAccountShares [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Share info

    + Attributes (array[accountShare])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### shareAccount [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Shares Request)



+ Response 200 (application/json)

        Share info

    + Attributes (accountShare)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Shares By AccountUid [/v2/accounts/{accountUid}/shares/{shareUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + shareUid (string, required)

        Share UID

        + Sample: Abc_123-XY

### getShare [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Share info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### updateShare [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Shares Request1)



+ Response 200 (application/json)

        Share info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteShare [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Share info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Shares Permissions [/v2/accounts/{accountUid}/shares/{shareUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + shareUid (string, required)

        Share UID

        + Sample: Abc_123-XY

### getSharePermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (sharepropertiespermissions)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Shares By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/shares{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### listFileShares [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Share info

    + Attributes (array[fileShare])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### shareFile [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Files Shares Request)



+ Response 200 (application/json)

        Share info

    + Attributes (fileShare)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Shares By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}/shares{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### listFolderShares [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Share info

    + Attributes (array[folderShare])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### shareFolder [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Folders Shares Request)



+ Response 200 (application/json)

        Share info

    + Attributes (folderShare)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Tags Shares By AccountUid [/v2/accounts/{accountUid}/tags/{tagUid}/shares{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + tagUid (string, required)

        Tag UID

        + Sample: Abc_123-XY

### listTagShares [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Share info

    + Attributes (array[tagShare])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### shareTag [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Tags Shares Request)



+ Response 200 (application/json)

        Share info

    + Attributes (tagShare)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Access Public By AccountUid [/v2/accounts/{accountUid}/access/public{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAllPublicShares [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Array of Shares

    + Attributes (array[string])



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Access Unaccepted [/v2/accounts/{accountUid}/access/unaccepted]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAllUnacceptedShareEmails [GET]

+ Response 200 (application/json)

        Array of emails

    + Attributes (array[string])



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Group

Group-related operations

## V2 Accounts Groups By AccountUid [/v2/accounts/{accountUid}/groups{?fields,page,per-page,sort,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getGroups [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order

        + Sample: ["name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example groups.groups)

        + Sample: ["shares","users"]
        + Members
            + `users` 
            + `groups` 
            + `shares` 


+ Response 200 (application/json)

        Array of groups

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups By AccountUid [/v2/accounts/{accountUid}/groups{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### addGroup [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Groups Request)



+ Response 200 (application/json)

        Created group

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### getGroup [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example groups.groups)

        + Sample: ["shares","users"]
        + Members
            + `users` 
            + `groups` 
            + `shares` 


+ Response 200 (application/json)

        Group info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### updateGroup [PUT]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (V2 Accounts Groups Request)



+ Response 200 (application/json)

        Group info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)



### deleteGroup [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Group info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Permissions [/v2/accounts/{accountUid}/groups/{groupUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### getGroupPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (grouppropertiespermissions)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Shares By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}/shares{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### getGroupSharedItems [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example entity.meta).

        + Members
            + `entity` 
            + `account` 
            + `tag` 


+ Response 200 (application/json)

        Item shares

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Members By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}/members{?fields,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### getGroupMembers [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Group members

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Members By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}/members{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### addGroupMember [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        Member info

    + Attributes (string)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Members MemberUid [/v2/accounts/{accountUid}/groups/{groupUid}/members/{memberUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY
    + memberUid (string, required)

        Member UID

        + Sample: Abc_123-XY

### deleteGroupMember [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Member info

    + Attributes (string)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Users By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}/users{?fields,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### getGroupUsers [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Group members (users)

    + Attributes (array[Group member user])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Users By AccountUid [/v2/accounts/{accountUid}/groups/{groupUid}/users{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### addGroupUser [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (string)



+ Response 200 (application/json)

        User info

    + Attributes (Other user)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Users UserUid [/v2/accounts/{accountUid}/groups/{groupUid}/users/{userUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### deleteGroupUser [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        User info

    + Attributes (Other user)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Users UserUid Permissions [/v2/accounts/{accountUid}/groups/{groupUid}/users/{userUid}/permissions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY
    + userUid (string, required)

        User UID

        + Sample: Abc_123-XY

### getGroupUserPermissions [GET]

+ Response 200 (application/json)

        Permissions

    + Attributes (V2 Accounts Groups Users UserUid Permissions Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Children [/v2/accounts/{accountUid}/groups/{groupUid}/children{?fields,page,per-page}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### getGroupChildren [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 50
        + Sample: 10

+ Response 200 (application/json)

        Group members (groups)

    + Attributes (array[Group member child])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Children [/v2/accounts/{accountUid}/groups/{groupUid}/children{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY

### addGroupChild [POST]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request (<<Add Header Value>>)
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>


    + Attributes (By uid)



+ Response 200 (application/json)

        Member info

    + Attributes (Group member child)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 422 (application/json)

        Unprocessable entity

    + Attributes (422unprocessableEntity1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Groups Children ChildUid [/v2/accounts/{accountUid}/groups/{groupUid}/children/{childUid}{?fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + groupUid (string, required)

        Group UID

        + Sample: Abc_123-XY
    + childUid (string, required)

        Child UID

        + Sample: Abc_123-XY

### deleteGroupChild [DELETE]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Request 
    + Headers

            X-Folderit-Request-Key:<<Add Header Value>>





+ Response 200 (application/json)

        Member info (group)

    + Attributes (Group member child)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Plan

Plan-related operations

## V2 Plans [/v2/plans{?fields}]

### getPlans [GET]
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Plan array

    + Attributes (array[Plan])



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Search

Search-related operations

## V2 Accounts Search Simple By AccountUid [/v2/accounts/{accountUid}/search/simple{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### searchAutocomplete [GET]
Alternatively, POST request is also supported
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.meta)

        + Members
            + `entity` 
            + `folder` 
            + `file` 
            + `link` 
            + `asic` 
            + `highlight` 


+ Response 200 (application/json)

        Results

    + Attributes (V2 Accounts Search Simple Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Search Entities [/v2/accounts/{accountUid}/search/entities{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### searchEntities [GET]
Alternatively, POST request is also supported
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.meta)

        + Members
            + `entity` 
            + `folder` 
            + `file` 
            + `link` 
            + `asic` 
            + `highlight` 


+ Response 200 (application/json)

        Results

    + Attributes (V2 Accounts Search Entities Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Search Files By AccountUid [/v2/accounts/{accountUid}/search/files{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### searchFiles [GET]
Alternatively, POST request is also supported
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.meta)

        + Members
            + `entity` 
            + `folder` 
            + `file` 
            + `link` 
            + `asic` 
            + `highlight` 


+ Response 200 (application/json)

        Results

    + Attributes (V2 Accounts Search Files Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Search Folders By AccountUid [/v2/accounts/{accountUid}/search/folders{?fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### searchFolders [GET]
Alternatively, POST request is also supported
+ Parameters
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained (for example file.meta)

        + Members
            + `entity` 
            + `folder` 
            + `file` 
            + `link` 
            + `asic` 
            + `highlight` 


+ Response 200 (application/json)

        Results

    + Attributes (V2 Accounts Search Folders Response)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 404 (application/json)

        Not Found

    + Attributes (404notFound1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Search Extensions [/v2/accounts/{accountUid}/search/extensions]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getUsedFileExtensions [GET]

+ Response 200 (application/json)

        Extensions in use on an account

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Group Audit

Audit-related operations

## V2 Accounts Audit AccountLog [/v2/accounts/{accountUid}/audit/accountLog{?page,per-page,sort,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccountRecentAuditTrail [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + sort (array[string], optional)

        Default sort: `-id`. If request body contains `id` or `uid` parameter then the default sort is `id`

        + Members
            + `id` 
            + `-id` 

    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["activeUser"]
        + Members
            + `activeUser` 


+ Response 200 (application/json)

        Audit Trail entries

    + Attributes (array[string])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Audit By AccountUid [/v2/accounts/{accountUid}/audit{?page,per-page,sort,fields,expand}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getAccountAuditTrail [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order

        + Sample: ["name"]
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]
    + expand (array[string], optional)

        Include related information (comma-separated). Can be chained using a dot.

        + Sample: ["activeUser"]
        + Members
            + `activeUser` 


+ Response 200 (application/json)

        Audit Trail entries

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Files Audit By AccountUid [/v2/accounts/{accountUid}/files/{fileUid}/audit{?page,per-page,sort,fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + fileUid (string, required)

        File UID

        + Sample: Abc_123-XY

### getFileAuditTrail [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order

        + Sample: ["name"]
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Audit Trail entries

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Folders Audit By AccountUid [/v2/accounts/{accountUid}/folders/{folderUid}/audit{?page,per-page,sort,fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY
    + folderUid (string, required)

        Folder UID

        + Sample: Abc_123-XY

### getFolderAuditTrail [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + sort (array[string], optional)

        Pagination sorting, comma-separated list of attribute names, prepend with hyphen for descending order

        + Sample: ["name"]
    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Audit Trail entries

    + Attributes (array[string])



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)




## V2 Accounts Audit SyncLog By AccountUid [/v2/accounts/{accountUid}/audit/syncLog{?page,per-page,sort,fields}]

+ Parameters
    + accountUid (string, required)

        Account UID

        + Sample: Abc_123-XY

### getFileSyncLog [GET]
+ Parameters
    + page (number, optional)

        Pagination page

        + Sample: 1
    + per-page (number, optional) -

        Pagination page size

        + Default: 500
        + Sample: 10
    + sort (array[string], optional)

        Default sort: `-id`. If request body contains `id` or `uid` parameter then the default sort is `id`

        + Members
            + `id` 
            + `-id` 

    + fields (array[string], optional)

        Include only listed fields in response (comma-separated). Use asterisk (*) for including all original fields (useful for combining with `expand`). Can be chained (for example field.subField).

        + Sample: ["uid","name"]

+ Response 200 (application/json)

        Audit Trail entries

    + Attributes (array[File entries])



+ Response 400 (application/json)

        Bad Request

    + Attributes (400badRequest1)



+ Response 403 (application/json)

        Forbidden

    + Attributes (403forbidden1)



+ Response 401 (application/json)

        Unauthorized

    + Attributes (401unauthorized1)





# Data Structures

## User provider (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `providerId`: `google` (string, optional) 
+ `providerValue`: `johnSmith@gmail.com` (string, optional) 
+ `providerName`: `John Smith` (string, optional) 


## User (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name` (string, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `locale`: `en-GB` (string, optional) 
+ `planId`: `10` (number, optional) 
+ `companyName`: `My company name` (string, optional) 
+ `phone`: `+123 456 7890` (string, optional, nullable) 
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, optional) 
+ `taxNo`: `EE123456789` (string, optional, nullable) 
+ `registerNo`: `12345678` (string, optional, nullable) 
+ `title`: `Mr.` (string, optional, nullable) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `userType` (enum[string], optional) 
    + `private`
    + `company`
+ `defaultAccountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `timezone`: `Europe/London` (string, optional) 
+ `providers` (array[User provider], optional) 


## User with expand fields (object)


### Properties
+ `dateFormat` (string, optional) 
+ `timeFormat` (string, optional) 


## User with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name` (string, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `locale`: `en-GB` (string, optional) 
+ `planId`: `10` (number, optional) 
+ `companyName`: `My company name` (string, optional) 
+ `phone`: `+123 456 7890` (string, optional, nullable) 
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, optional) 
+ `taxNo`: `EE123456789` (string, optional, nullable) 
+ `registerNo`: `12345678` (string, optional, nullable) 
+ `title`: `Mr.` (string, optional, nullable) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `userType` (enum[string], optional) 
    + `private`
    + `company`
+ `defaultAccountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `timezone`: `Europe/London` (string, optional) 
+ `providers` (array[User provider], optional) 
+ `dateFormat` (string, optional) 
+ `timeFormat` (string, optional) 


## Private (object)


### Properties
+ `userType` (enum[string], required) 
    + `private`
    + `company`
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, optional) 
+ `name` (string, optional) 


## userBillingBase (object)


### Properties
+ `userType` (enum[string], required) 
    + `private`
    + `company`
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, optional) 


## Company (object)


### Properties
+ `userType` (enum[string], required) 
    + `private`
    + `company`
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, optional) 
+ `name` (string, optional, nullable) 
+ `companyName`: `My company name` (string, optional) 
+ `taxNo`: `EE123456789` (string, optional, nullable) 
+ `registerNo`: `12345678` (string, optional, nullable) 


## User signup (object)


### Properties
+ `email`: `someEmail@foo.bar` (string, required) 
+ `companyName`: `My company name` (string, optional) 
+ `phone`: `+123 456 7890` (string, optional, nullable) 
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, required) 
+ `taxNo`: `EE123456789` (string, optional, nullable) 
+ `registerNo`: `12345678` (string, optional, nullable) 
+ `title`: `Mr.` (string, optional, nullable) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `planId`: `10` (number, optional) 
+ `locale`: `en-GB` (string, optional) 
+ `timezone`: `Europe/London` (string, optional) 
+ `defaultAccountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `options` (array[Generic action], optional) 


## newUserSignup (object)


### Properties
+ `email`: `someEmail@foo.bar` (string, required) 
+ `companyName`: `My company name` (string, required) 
+ `phone`: `+123 456 7890` (string, optional, nullable) 
+ `address`: `Street name 10` (string, optional) 
+ `zip`: `12345` (string, optional) 
+ `city`: `City name` (string, optional) 
+ `state`: `State name` (string, optional) 
+ `countryCode` (string, required) 
+ `taxNo`: `EE123456789` (string, optional, nullable) 
+ `registerNo`: `12345678` (string, optional, nullable) 
+ `title`: `Mr.` (string, optional, nullable) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `planId`: `10` (number, optional) 
+ `locale`: `en-GB` (string, optional) 
+ `timezone`: `Europe/London` (string, optional) 
+ `defaultAccountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `options` (array[Generic action], optional) 


## Other user (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - User UID
+ `name` (string, optional) - User name
+ `email`: `someEmail@foo.bar` (string, optional) - User email
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 


## Team user (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name` (string, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 


## Group (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `name`: `My group` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (grouppropertiespermissions, optional) 


## Share settings (object)


### Properties
+ `isRecursive`: `true` (boolean, optional) - Apply these settings to entities below current target level
+ `accountRead`: `false` (boolean, optional) - View account-related data (groups, meta, ...)
+ `accountDownload`: `false` (boolean, optional) - Download whole account contents at once
+ `accountUpdate`: `false` (boolean, optional) - Update account settings, groups, ...
+ `accountShare`: `false` (boolean, optional) - Share account to other users
+ `accountAudit`: `false` (boolean, optional) - View account audit data
+ `accountGroups`: `false` (boolean, optional) - Manage groups
+ `accountUsers`: `false` (boolean, optional) - Manage users
+ `accountMeta`: `false` (boolean, optional) - Manage meta
+ `accountRecycleBin`: `false` (boolean, optional) - Manage recycle bin
+ `accountNumbering`: `false` (boolean, optional) - Manage numbering
+ `accountIdp`: `false` (boolean, optional) - Manage IdP
+ `accountPasswordPolicy`: `false` (boolean, optional) - Manage password policy
+ `folderCreate`: `false` (boolean, optional) - Create a new folder
+ `folderRead`: `true` (boolean, optional) - List folders in a folder
+ `folderDownload`: `true` (boolean, optional) - Download a folder
+ `folderUpdate`: `false` (boolean, optional) - Update folder data
+ `folderMove`: `false` (boolean, optional) - Move folder to new location (requires folderCreate permission at new location)
+ `folderDelete`: `false` (boolean, optional) - Delete a folder
+ `folderDuplicate`: `false` (boolean, optional) - Duplicate folder (requires folderCreate permission at target)
+ `folderShare`: `false` (boolean, optional) - Share a folder
+ `folderPublicShare`: `false` (boolean, optional) - Create a public link for a folder (requires folderShare permission)
+ `folderAudit`: `true` (boolean, optional) - View folder audit trail
+ `fileCreate`: `true` (boolean, optional) - Add a file
+ `fileRead`: `true` (boolean, optional) - List files in a folder
+ `filePreview`: `true` (boolean, optional) - View preview of a file
+ `fileDownload`: `true` (boolean, optional) - Download a file
+ `fileUpdate`: `false` (boolean, optional) - Update name, meta, ... of a file
+ `fileUpdateContent`: `false` (boolean, optional) - Manage file versions
+ `fileMove`: `false` (boolean, optional) - Move file to new location (requires fileCreate permission at new location)
+ `fileDelete`: `false` (boolean, optional) - Delete a file
+ `fileDuplicate`: `false` (boolean, optional) - Duplicate file (requires fileCreate permission at target)
+ `fileShare`: `false` (boolean, optional) - Share a file
+ `filePublicShare`: `false` (boolean, optional) - Create a public link for a file (requires folderShare permission)
+ `fileAudit`: `true` (boolean, optional) - View file audit trail


## Share (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - UID of shared item, be it account, entity (file, folder, ...), tag...
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `public` (boolean, optional) 
+ `link` (string, optional, nullable) - URL to shared entity, null if not a public share
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `status` (enum[string], optional) 
    + `ACTIVE`
    + `DEACTIVATED`
    + `PENDING`
    + `DELETED`
+ `group`: `My group` (string, optional, nullable) - null, if not a group share
+ `groupUid` (string, optional, nullable) 
+ `email` (string, optional, nullable) 
+ `userUid` (string, optional, nullable) 
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `settings` (Share settings, optional) 


## Group with expanded fields (object)


### Properties
+ `users` (array[Other user], optional) 
+ `groups` (array[Group], optional) 
+ `shares` (array[Share], optional) 


## Group with expanded fields2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `name`: `My group` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (grouppropertiespermissions, optional) 
+ `users` (array[Other user], optional) 
+ `groups` (array[Group], optional) 
+ `shares` (array[Share], optional) 


## Team user with expanded fields (object)


### Properties
+ `remoteUsername`: `someEmail@foo.bar` (string, optional, nullable) 
+ `status` (enum[string], optional, nullable) 
    + `active`
    + `inactive`
    + `invite`
    + `invite-inactive`
+ `groups` (array[string], optional) 
+ `position` (string, optional, nullable) 


## Team user with expanded fields2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name` (string, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `remoteUsername`: `someEmail@foo.bar` (string, optional, nullable) 
+ `status` (enum[string], optional, nullable) 
    + `active`
    + `inactive`
    + `invite`
    + `invite-inactive`
+ `groups` (array[string], optional) 
+ `position` (string, optional, nullable) 


## Account (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `Account name` (string, optional) 
+ `owner` (Other user, optional) 
+ `canGetSections` (boolean, optional) 
+ `inboxFolderUid` (string, optional, nullable) 
+ `inboxEmailName` (string, optional, nullable) 
+ `teamFolderUid` (string, optional, nullable) 
+ `indexing`: `true` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `language`: `eng` (string, optional) - OCR language
+ `permissions` (permissions, optional) 


## accountShare (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - UID of shared item, be it account, entity (file, folder, ...), tag...
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `public` (boolean, optional) 
+ `link` (string, optional, nullable) - URL to shared entity, null if not a public share
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `status` (enum[string], optional) 
    + `ACTIVE`
    + `DEACTIVATED`
    + `PENDING`
    + `DELETED`
+ `group`: `My group` (string, optional, nullable) - null, if not a group share
+ `groupUid` (string, optional, nullable) 
+ `email` (string, optional, nullable) 
+ `userUid` (string, optional, nullable) 
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `settings` (Share settings, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 


## Account stats (object)


### Properties
+ `folders` (number, optional, nullable) 
+ `files` (number, optional, nullable) 
+ `size` (number, optional, nullable) 
+ `collaboratorCount` (number, optional, nullable) 
+ `time`: `1537177837` (number, optional) - UNIX timestamp


## Account with expanded fields (object)


### Properties
+ `shares` (array[accountShare], optional) 
+ `stats` (Account stats, optional, nullable) 


## Account with expanded fields2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `Account name` (string, optional) 
+ `owner` (Other user, optional) 
+ `canGetSections` (boolean, optional) 
+ `inboxFolderUid` (string, optional, nullable) 
+ `inboxEmailName` (string, optional, nullable) 
+ `teamFolderUid` (string, optional, nullable) 
+ `indexing`: `true` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `language`: `eng` (string, optional) - OCR language
+ `permissions` (permissions, optional) 
+ `shares` (array[accountShare], optional) 
+ `stats` (Account stats, optional, nullable) 


## Partially shared account (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `Account name` (string, optional) 
+ `owner` (Other user, optional) 
+ `canGetSections` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp


## permissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 
+ `canDownload`: `true` (boolean, optional) 
+ `canCreateGroup`: `true` (boolean, optional) 
+ `canCreateMeta`: `true` (boolean, optional) 
+ `canCreateSection`: `true` (boolean, optional) 
+ `canAudit`: `true` (boolean, optional) 
+ `canShareCustomPermissions`: `true` (boolean, optional) 
+ `canManageGroups`: `true` (boolean, optional) 
+ `canManageUsers`: `true` (boolean, optional) 
+ `canManageMeta`: `true` (boolean, optional) 
+ `canManageRecycleBin`: `true` (boolean, optional) 
+ `canManageNumbering`: `true` (boolean, optional) 
+ `canManageIdp`: `true` (boolean, optional) 
+ `canManagePasswordPolicy`: `true` (boolean, optional) 


## Account User (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name` (string, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `position` (string, optional, nullable) 
+ `teamUser` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp


## Account User with expanded fields (object)


### Properties
+ `teamUserStatus` (enum[string], optional, nullable) 
    + `active`
    + `inactive`
    + `invite`
    + `invite-inactive`


## Account User with expanded fields2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name` (string, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `position` (string, optional, nullable) 
+ `teamUser` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `teamUserStatus` (enum[string], optional, nullable) 
    + `active`
    + `inactive`
    + `invite`
    + `invite-inactive`


## fileShare (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - UID of shared item, be it account, entity (file, folder, ...), tag...
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `public` (boolean, optional) 
+ `link` (string, optional, nullable) - URL to shared entity, null if not a public share
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `status` (enum[string], optional) 
    + `ACTIVE`
    + `DEACTIVATED`
    + `PENDING`
    + `DELETED`
+ `group`: `My group` (string, optional, nullable) - null, if not a group share
+ `groupUid` (string, optional, nullable) 
+ `email` (string, optional, nullable) 
+ `userUid` (string, optional, nullable) 
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `settings` (Share settings, optional) 
+ `fileUid`: `Abc_123-XY` (string, optional) 


## folderShare (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - UID of shared item, be it account, entity (file, folder, ...), tag...
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `public` (boolean, optional) 
+ `link` (string, optional, nullable) - URL to shared entity, null if not a public share
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `status` (enum[string], optional) 
    + `ACTIVE`
    + `DEACTIVATED`
    + `PENDING`
    + `DELETED`
+ `group`: `My group` (string, optional, nullable) - null, if not a group share
+ `groupUid` (string, optional, nullable) 
+ `email` (string, optional, nullable) 
+ `userUid` (string, optional, nullable) 
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `settings` (Share settings, optional) 
+ `folderUid`: `Abc_123-XY` (string, optional) 


## tagShare (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - UID of shared item, be it account, entity (file, folder, ...), tag...
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `public` (boolean, optional) 
+ `link` (string, optional, nullable) - URL to shared entity, null if not a public share
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `status` (enum[string], optional) 
    + `ACTIVE`
    + `DEACTIVATED`
    + `PENDING`
    + `DELETED`
+ `group`: `My group` (string, optional, nullable) - null, if not a group share
+ `groupUid` (string, optional, nullable) 
+ `email` (string, optional, nullable) 
+ `userUid` (string, optional, nullable) 
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `settings` (Share settings, optional) 
+ `tagUid`: `Abc_123-XY` (string, optional) 


## indexroot (object)


### Properties
+ `entities` (array[string], optional) 


## Entity (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `parentUid` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `name`: `My entity name` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting

+ `retentionIsCustom` (boolean, optional) - Indicates whether the retentionEnd is overridden from the default
+ `retention` (retention, optional, nullable) - Recycle/Permanently delete/Archive items after x time, counting from the time uploaded
+ `activeRetention` (activeRetention, optional, nullable) 


## Attribute Column (object)


### Properties
+ `field` (enum[string], optional) 
    + `name`
    + `tags`
    + `date`
    + `due_date`
    + `created_at`
    + `updated_at`
    + `signees`
    + `id`
    + `size`
    + `mail_sender`
    + `retention_end`
    + `language`
    + `number`
    + `note`
    + `creator`
    + `extension`
    + `approved_at`
    + `signed_at`
    + `acknowledged_at`


## retention (object)
Recycle/Permanently delete/Archive items after x time, counting from the time uploaded

### Properties
+ `time`: `1m5d` (string, required)

    Retention time order can matter as the durations are processed in the order they are stored  
    For example:  
    1m5d -> time uploaded + 1 month + 5 days  
    5d1m -> time uploaded + 5 days + 1 month  
    Format accepts input with spaces between durations (accepts both "1m5d" and "1m 5d" are valid), excess whitespace is trimmed automatically  
    Set time to 0 length string and action to null to force infinite retention

+ `action` (enum[string], required, nullable) 
    + `delete`
    + `purge`
    + `archive`
+ `isRecursive` (boolean, required, nullable) 
+ `target`: `Abc_123-XY` (string, optional) - Archival target
+ `keepStructure` (boolean, optional) - Keep folder structure on archiving with isRecursive active
+ `source` (enum[string], optional) - Source field for generating the end time
    + `createdAt`
    + `updatedAt`
    + `date`
    + `dueDate`
    + `approvedAt`
    + `signedAt`
    + `acknowledgedAt`
    + `reviewedAt`
    + `meta:<uid>`


## activeRetention (object)


### Properties
+ `time`: `1m5d` (string, required)

    Retention time order can matter as the durations are processed in the order they are stored  
    For example:  
    1m5d -> time uploaded + 1 month + 5 days  
    5d1m -> time uploaded + 5 days + 1 month  
    Format accepts input with spaces between durations (accepts both "1m5d" and "1m 5d" are valid), excess whitespace is trimmed automatically  
    Set time to 0 length string and action to null to force infinite retention

+ `action` (enum[string], required, nullable) 
    + `delete`
    + `purge`
    + `archive`
+ `entityUid`: `Abc_123-XY` (string, required) - UID of an entity from where the retention originates from
+ `isRecursive` (boolean, required, nullable) 
+ `source` (enum[string], optional) - Source field for generating the end time
    + `createdAt`
    + `updatedAt`
    + `date`
    + `dueDate`
    + `approvedAt`
    + `signedAt`
    + `acknowledgedAt`
    + `reviewedAt`
    + `meta:<uid>`
+ `target`: `Abc_123-XY` (string, optional) 
+ `keepStructure` (boolean, optional) - Keep folder structure on archiving with isRecursive active


## Mailin address (object)


### Properties
+ `address`: `some-random-address@folderit.com` (string, optional) 
+ `active` (boolean, optional) 


## Folder (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `parentUid` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `name`: `My folder name` (string, optional) 
+ `visibleColumns` (array[string], optional) - Array of columns to be shown by the client
+ `size`: `123` (number, optional) 
+ `language`: `eng` (string, optional) - OCR language
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retention` (retention, optional, nullable) - Recycle/Permanently delete/Archive items after x time, counting from the time uploaded
+ `activeRetention` (activeRetention, optional, nullable) 
+ `mailInAddress` (Mailin address, optional, nullable) 
+ `permissions` (folderpropertiespermissions, optional) 


## Meta Base (object)


### Properties
+ `type` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## String meta (object)


### Properties
+ `type`: `string` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity string meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (String meta, optional) 
+ `value`: `meta value` (string, optional) 


## Text meta (object)


### Properties
+ `type`: `text` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity text meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Text meta, optional) 
+ `value`: `meta value` (string, optional) 


## Integer meta (object)


### Properties
+ `type`: `integer` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity integer meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Integer meta, optional) 
+ `value`: `42` (number, optional) 


## Integer meta1 (object)


### Properties
+ `type`: `float` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `decimalScale` (number, optional) 


## Entity decimal meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Integer meta1, optional) 
+ `value`: `42.50` (string, optional) 
+ `scale` (number, optional) 


## Date meta (object)


### Properties
+ `type`: `date` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity date meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Date meta, optional) 
+ `value` (string, optional) 


## Datetime meta (object)


### Properties
+ `type`: `datetime` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity datetime meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Datetime meta, optional) 
+ `value` (string, optional) 


## Time meta (object)


### Properties
+ `type`: `time` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity time meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Time meta, optional) 
+ `value`: `17:32:28` (string, optional) 


## Boolean meta (object)


### Properties
+ `type`: `boolean` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) - Boolean meta does not support setting this property to `true`
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity boolean meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Boolean meta, optional) 
+ `value` (boolean, optional) 


## URL meta (object)


### Properties
+ `type`: `url` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity URL meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (URL meta, optional) 
+ `value` (string, optional) 


## Email meta (object)


### Properties
+ `type`: `email` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 


## Entity email meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Email meta, optional) 
+ `value` (string, optional) 


## Meta Select Option (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `value`: `meta name` (string, optional) 
+ `order`: `1` (number, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `isUsed` (boolean, optional) 


## Select meta (object)


### Properties
+ `type`: `select` (string, required) 
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `multiple`: `false` (boolean, optional) 
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `options` (array[Meta Select Option], optional) - Options list


## Entity select meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `meta` (Select meta, optional) 
+ `value`: `Abc_123-XY` (string, optional) - Option UID


## Entity1 (object)


### Properties
+ `number` (string, optional, nullable) 
+ `numberingSchemeUid` (string, optional, nullable) 
+ `mailSender` (string, optional, nullable) 
+ `mailSenderName` (string, optional, nullable) 
+ `mailContent` (string, optional, nullable) 
+ `mailSubject` (string, optional, nullable) 
+ `note` (string, optional, nullable) 
+ `date` (string, optional, nullable) 
+ `dueDate` (string, optional, nullable) 


## FolderStats (object)


### Properties
+ `folders`: `1` (number, optional) 
+ `files`: `1` (number, optional) 
+ `missingMeta`: `1` (number, optional) 
+ `size`: `3` (number, optional) 
+ `time`: `1537177837` (number, optional) - UNIX timestamp


## Folder with expanded fields2 (object)


### Properties
+ `entity` (string, optional) 
+ `parent` (Folder, optional, nullable) 
+ `meta` (array[string], optional) 
+ `shares` (array[folderShare], optional) 
+ `extra` (Entity1, optional) 
+ `sectionOrder` (number, optional, nullable) - User-configurable value for manually ordering sections
+ `inboxFolder` (boolean, optional) 
+ `teamFolder` (boolean, optional) 
+ `folderStats` (FolderStats, optional) 
+ `visibleColumnsWithMeta` (array[string], optional) - Array of columns to be shown by the client
+ `sort`: `name` (string, optional) - Column names, prefixed with a dash (`-`) for descending order, separated with a comma
+ `viewType` (enum[string], optional) - View type of a folder listing
    + `list`
    + `thumbnail`


## Folder with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `parentUid` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `name`: `My folder name` (string, optional) 
+ `visibleColumns` (array[string], optional) - Array of columns to be shown by the client
+ `size`: `123` (number, optional) 
+ `language`: `eng` (string, optional) - OCR language
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retention` (retention, optional, nullable) - Recycle/Permanently delete/Archive items after x time, counting from the time uploaded
+ `activeRetention` (activeRetention, optional, nullable) 
+ `mailInAddress` (Mailin address, optional, nullable) 
+ `permissions` (folderpropertiespermissions, optional) 
+ `entity` (string, optional) 
+ `parent` (Folder, optional, nullable) 
+ `meta` (array[string], optional) 
+ `shares` (array[folderShare], optional) 
+ `extra` (Entity1, optional) 
+ `sectionOrder` (number, optional, nullable) - User-configurable value for manually ordering sections
+ `inboxFolder` (boolean, optional) 
+ `teamFolder` (boolean, optional) 
+ `folderStats` (FolderStats, optional) 
+ `visibleColumnsWithMeta` (array[string], optional) - Array of columns to be shown by the client
+ `sort`: `name` (string, optional) - Column names, prefixed with a dash (`-`) for descending order, separated with a comma
+ `viewType` (enum[string], optional) - View type of a folder listing
    + `list`
    + `thumbnail`


## retentionEnd (object)
Recycle/permanently delete/archive entity at end time  
Setting retentionEnd object to null restores the default "inherit from parent" setting

### Properties
+ `end`: `1537177837` (number, required, nullable) - null value for both end and action results in forced infinite retention
+ `action` (enum[string], required, nullable) 
    + `delete`
    + `purge`
    + `archive`
+ `target`: `Abc_123-XY` (string, optional) - Archival target
+ `source` (enum[string], optional, nullable) - Indicates the source field for generating the end time
    + `createdAt`
    + `updatedAt`
    + `date`
    + `dueDate`
    + `approvedAt`
    + `signedAt`
    + `acknowledgedAt`
    + `reviewedAt`
    + `meta:<uid>`


## File (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `folderUid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `name`: `My entity name` (string, optional) 
+ `extension`: `txt` (string, optional) 
+ `mimeType`: `text/plain` (string, optional) 
+ `size`: `123` (number, optional) 
+ `number` (string, optional, nullable) 
+ `versionUid`: `Abc_123-XY` (string, optional) 
+ `numberingSchemeUid` (string, optional, nullable) 
+ `date` (string, optional, nullable) 
+ `dueDate` (string, optional, nullable) 
+ `note` (string, optional, nullable) 
+ `isLocked` (boolean, optional) - A file could be locked without specific user, in that case the lockedBy attribute will stay null
+ `lockId` (string, optional, nullable) 
+ `lockedBy` (string, optional, nullable) 
+ `lockTime` (number, optional, nullable) 
+ `isSentByMail` (boolean, optional) 
+ `mailSender` (string, optional, nullable) 
+ `mailSenderName` (string, optional, nullable) 
+ `mailContent` (string, optional, nullable) 
+ `mailSubject` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `previewAvailable` (boolean, optional) - Indicates whether preview is available for the current version
+ `hash` (hash, optional) 
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting

+ `retentionIsCustom` (boolean, optional) - Indicates whether the retentionEnd is overridden from the default
+ `language`: `eng` (string, optional) - OCR language
+ `approvalStatus` (enum[string], optional, nullable) 
    + `active`
    + `inProgress`
    + `approved`
    + `rejected`
    + `deleted`
+ `permissions` (propertiespermissions, optional) 
+ `supports` (Supports, optional) 


## Tag (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `name`: `myTag` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `parentUid` (string, optional, nullable) 
+ `permissions` (metaBasepropertiespermissions, optional) 


## Person (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `name`: `Johnny Smith` (string, optional) 
+ `permissions` (metaBasepropertiespermissions, optional) 


## Reminder1 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `fileUid`: `Abc_123-XY` (string, optional) - File UID
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `remindAt`: `1537177837` (string, optional) - Unix timestamp
+ `content`: `Hello` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `recurrence` (enum[string], optional, nullable) 
    + `daily`
    + `weekly`
    + `monthly`
    + `quarterly`
    + `semi-yearly`
    + `yearly`
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp


## reminder (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `fileUid`: `Abc_123-XY` (string, optional) - File UID
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `remindAt`: `1537177837` (string, optional) - Unix timestamp
+ `content`: `Hello` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `recurrence` (enum[string], optional, nullable) 
    + `daily`
    + `weekly`
    + `monthly`
    + `quarterly`
    + `semi-yearly`
    + `yearly`
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `groupUid`: `Abc_123-XY` (string, optional, nullable) - Group UID
+ `email`: `someEmail@foo.bar` (string, optional) - Email


## Related item (object)


### Properties
+ `fileUid` (string, required, nullable) 
+ `entityUid`: `Abc_123-XY` (string, required) 
+ `user` (Other user, required) 
+ `createdAt`: `1537177837` (number, required) - UNIX timestamp
+ `type` (enum[string], required) 
    + `folder`
    + `file`
    + `link`


## hash (object)


### Properties
+ `md5`: `d571828bd7c88787c98cf0112f8cc7f1` (string, optional) 
+ `sha256`: `8a9f51d294e88ee1c600452542397cdac4c1064972e000c198e83e9a5052f488` (string, optional) 


## File version (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `filename`: `sample.txt` (string, optional) 
+ `fileUid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `mimeType`: `text/plain` (string, optional) 
+ `previewAvailable` (boolean, optional) - Indicates whether preview is available for the version
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `hash` (hash, optional) 
+ `approvalStatus` (enum[string], optional) 
    + `active`
    + `inProgress`
    + `approved`
    + `rejected`
    + `deleted`
+ `size`: `123` (number, optional) 


## File thumbnails (object)


### Properties
+ `avif_128` (string, optional) 
+ `avif_400` (string, optional) 
+ `jpg_128` (string, optional) 
+ `jpg_400` (string, optional) 


## File version with expanded fields2 (object)


### Properties
+ `isActive` (boolean, optional) - Indicates if the version is active for the file
+ `thumbnails` (File thumbnails, optional) 


## File version with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `filename`: `sample.txt` (string, optional) 
+ `fileUid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `mimeType`: `text/plain` (string, optional) 
+ `previewAvailable` (boolean, optional) - Indicates whether preview is available for the version
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `hash` (hash, optional) 
+ `approvalStatus` (enum[string], optional) 
    + `active`
    + `inProgress`
    + `approved`
    + `rejected`
    + `deleted`
+ `size`: `123` (number, optional) 
+ `isActive` (boolean, optional) - Indicates if the version is active for the file
+ `thumbnails` (File thumbnails, optional) 


## File with expanded fields2 (object)


### Properties
+ `entity` (string, optional) 
+ `tags` (array[Tag], optional) 
+ `people` (array[Person], optional) 
+ `meta` (array[string], optional) 
+ `reminders` (array[reminder], optional) 
+ `parent` (Folder, optional, nullable) 
+ `shares` (array[fileShare], optional) 
+ `fileRelations` (array[Related item], optional) 
+ `version` (string, optional) 
+ `thumbnails` (File thumbnails, optional) 


## File with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `folderUid`: `Abc_123-XY` (string, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `name`: `My entity name` (string, optional) 
+ `extension`: `txt` (string, optional) 
+ `mimeType`: `text/plain` (string, optional) 
+ `size`: `123` (number, optional) 
+ `number` (string, optional, nullable) 
+ `versionUid`: `Abc_123-XY` (string, optional) 
+ `numberingSchemeUid` (string, optional, nullable) 
+ `date` (string, optional, nullable) 
+ `dueDate` (string, optional, nullable) 
+ `note` (string, optional, nullable) 
+ `isLocked` (boolean, optional) - A file could be locked without specific user, in that case the lockedBy attribute will stay null
+ `lockId` (string, optional, nullable) 
+ `lockedBy` (string, optional, nullable) 
+ `lockTime` (number, optional, nullable) 
+ `isSentByMail` (boolean, optional) 
+ `mailSender` (string, optional, nullable) 
+ `mailSenderName` (string, optional, nullable) 
+ `mailContent` (string, optional, nullable) 
+ `mailSubject` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `previewAvailable` (boolean, optional) - Indicates whether preview is available for the current version
+ `hash` (hash, optional) 
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting

+ `retentionIsCustom` (boolean, optional) - Indicates whether the retentionEnd is overridden from the default
+ `language`: `eng` (string, optional) - OCR language
+ `approvalStatus` (enum[string], optional, nullable) 
    + `active`
    + `inProgress`
    + `approved`
    + `rejected`
    + `deleted`
+ `permissions` (propertiespermissions, optional) 
+ `supports` (Supports, optional) 
+ `entity` (string, optional) 
+ `tags` (array[Tag], optional) 
+ `people` (array[Person], optional) 
+ `meta` (array[string], optional) 
+ `reminders` (array[reminder], optional) 
+ `parent` (Folder, optional, nullable) 
+ `shares` (array[fileShare], optional) 
+ `fileRelations` (array[Related item], optional) 
+ `version` (string, optional) 
+ `thumbnails` (File thumbnails, optional) 


## Link (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `folderUid` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `name`: `My link name` (string, optional) 
+ `address` (string, optional) 
+ `embedded` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retentionIsCustom` (boolean, optional) - Indicates whether the retentionEnd is overridden from the default
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting

+ `permissions` (Permissions9, optional) 


## Resolution (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - Entity UID
+ `fileVersionUid` (string, optional, nullable) - File version UID
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `status` (enum[string], optional) 
    + `preparing`
    + `active`
    + `inProgress`
    + `complete`
    + `rejected`
+ `method` (enum[string], optional) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`
+ `type` (enum[string], optional) 
    + `parallel`
    + `serial`
    + `custom`
+ `note` (string, optional, nullable) 
+ `showMeta` (boolean, optional) 
+ `showRelatedEntities` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `initiatorUid`: `Abc_123-XY` (string, optional) 
+ `lastActivityAt` (number, optional, nullable) 
+ `timeoutAt` (number, optional, nullable) 
+ `isWorkflowManaged` (boolean, optional) 


## Resolution with expanded fields2 (object)


### Properties
+ `permissions` (Permissions10, optional) 
+ `entity` (string, optional) 
+ `fileVersion` (string, optional) 


## Resolution with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid`: `Abc_123-XY` (string, optional) - Entity UID
+ `fileVersionUid` (string, optional, nullable) - File version UID
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `status` (enum[string], optional) 
    + `preparing`
    + `active`
    + `inProgress`
    + `complete`
    + `rejected`
+ `method` (enum[string], optional) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`
+ `type` (enum[string], optional) 
    + `parallel`
    + `serial`
    + `custom`
+ `note` (string, optional, nullable) 
+ `showMeta` (boolean, optional) 
+ `showRelatedEntities` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `initiatorUid`: `Abc_123-XY` (string, optional) 
+ `lastActivityAt` (number, optional, nullable) 
+ `timeoutAt` (number, optional, nullable) 
+ `isWorkflowManaged` (boolean, optional) 
+ `permissions` (Permissions10, optional) 
+ `entity` (string, optional) 
+ `fileVersion` (string, optional) 


## Workflow (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid` (string, optional, nullable) 
+ `name` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `isActive` (boolean, optional) 
+ `isRecursive` (boolean, optional) - Not applicable without entityUid
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp


## Version (object)


### Properties
+ `id`: `1` (number, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `isActive` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp


## stepBase (object)


### Properties
+ `type` (string, required) 
+ `params` (object, optional) 


## moveStep (object)


### Properties
+ `type`: `move` (string, required) 
+ `params` (Params, required) 


## resolutionStep (object)


### Properties
+ `type`: `resolution` (string, required) 
+ `params` (Params1, required) 
+ `onFailure` (array[string], optional) 
+ `onSuccess` (array[string], optional) 
+ `onTimeout` (array[string], optional) 


## addInvitesStep (object)


### Properties
+ `type`: `addInvites` (string, required) 
+ `params` (Params2, required) 
+ `onFailure` (array[string], optional) 
+ `onSuccess` (array[string], optional) 
+ `onTimeout` (array[string], optional) 


## resolutionCompleteStep (object)


### Properties
+ `type`: `resolutionComplete` (string, required) 
+ `params` (Params3, required) 


## delayStep (object)


### Properties
+ `type`: `delay` (string, required) 
+ `params` (Params4, required) 


## Version with expanded fields2 (object)


### Properties
+ `steps` (array[string], optional) 


## Version with expanded fields (object)


### Properties
+ `id`: `1` (number, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `isActive` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `steps` (array[string], optional) 


## Source of event (object)


### Properties
+ `resolution` (Resolution1, optional) 


## Entity name (object)


### Properties
+ `caseSensitive` (boolean, required) 
+ `operator` (enum[string], required) 
    + `contains`
    + `equals`
+ `value` (string, required) 


## Trigger (object)


### Properties
+ `id`: `1` (number, optional) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `events` (array, optional) 
    + (enum[string])
        + Members
            + `entityNew`
            + `entityDelete`
            + `entityRestore`
            + `entityMove`
            + `entityName`
            + `entityNote`
            + `entityDescription`
            + `entityNumber`
            + `entityDate`
            + `entityDueDate`
            + `entityMetaNew`
            + `entityMetaDelete`
            + `entityMetaValue`
            + `entityTagNew`
            + `entityTagDelete`
            + `entityRelationNew`
            + `entityRelationDelete`
            + `fileVersionNew`
            + `fileVersionDelete`
            + `fileVersionRestore`
            + `fileSigneeNew`
            + `fileSigneeDelete`
            + `folderLanguage`
            + `folderMetaUpdate`
            + `linkAddress`
            + `linkEmbedded`
            + `resolutionResponse`
            + `resolutionComplete`
            + `shareNew`
            + `shareDelete`
            + `shareUpdate`
+ `condition` (array[string], optional) 


## Workflow with expanded fields2 (object)


### Properties
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `entity` (string, optional) 
+ `version` (string, optional, nullable) 
+ `versions` (array[string], optional) 
+ `triggers` (array[Trigger], optional) 


## Workflow with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `entityUid` (string, optional, nullable) 
+ `name` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) - Account UID
+ `isActive` (boolean, optional) 
+ `isRecursive` (boolean, optional) - Not applicable without entityUid
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `permissions` (metaBasepropertiespermissions, optional) 
+ `entity` (string, optional) 
+ `version` (string, optional, nullable) 
+ `versions` (array[string], optional) 
+ `triggers` (array[Trigger], optional) 


## Entity with expanded fields2 (object)


### Properties
+ `folder` (string, optional, nullable) 
+ `file` (string, optional, nullable) 
+ `link` (Link, optional, nullable) 
+ `asic` (string, optional, nullable) 
+ `shares` (array[Share], optional) 
+ `extra` (Entity1, optional) 
+ `meta` (array[string], optional) 
+ `account` (Account, optional) 
+ `sectionOrder` (number, optional, nullable) - User-configurable value for manually ordering sections
+ `inboxFolder` (boolean, optional) 
+ `teamFolder` (boolean, optional) 
+ `folderStats` (FolderStats, optional) 
+ `isRoot` (boolean, optional) 
+ `tags` (array[Tag], optional) 
+ `extension`: `txt` (string, optional) 
+ `size`: `123` (number, optional, nullable) 
+ `creator` (Other user, optional) 
+ `icon` (enum[string], optional) 
    + `folder`
    + `file`
    + `picture`
    + `chart`
    + `asic`
    + `link`
+ `reminders` (array[reminder], optional) 
+ `entityRelations` (array[Related item], optional) 
+ `resolutions` (array[string], optional) 
+ `resolutionSupport`: `{"approval":true,"acknowledgement":true,"esign":false}` (boolean, optional) - List of supported resolution methods, which could be initiated on this entity
+ `workflows` (array[string], optional) 


## Entity with expanded fields (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `parentUid` (string, optional, nullable) 
+ `accountUid`: `Abc_123-XY` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `name`: `My entity name` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `deletedAt` (number, optional, nullable) 
+ `purgeTime` (number, optional, nullable) 
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting

+ `retentionIsCustom` (boolean, optional) - Indicates whether the retentionEnd is overridden from the default
+ `retention` (retention, optional, nullable) - Recycle/Permanently delete/Archive items after x time, counting from the time uploaded
+ `activeRetention` (activeRetention, optional, nullable) 
+ `folder` (string, optional, nullable) 
+ `file` (string, optional, nullable) 
+ `link` (Link, optional, nullable) 
+ `asic` (string, optional, nullable) 
+ `shares` (array[Share], optional) 
+ `extra` (Entity1, optional) 
+ `meta` (array[string], optional) 
+ `account` (Account, optional) 
+ `sectionOrder` (number, optional, nullable) - User-configurable value for manually ordering sections
+ `inboxFolder` (boolean, optional) 
+ `teamFolder` (boolean, optional) 
+ `folderStats` (FolderStats, optional) 
+ `isRoot` (boolean, optional) 
+ `tags` (array[Tag], optional) 
+ `extension`: `txt` (string, optional) 
+ `size`: `123` (number, optional, nullable) 
+ `creator` (Other user, optional) 
+ `icon` (enum[string], optional) 
    + `folder`
    + `file`
    + `picture`
    + `chart`
    + `asic`
    + `link`
+ `reminders` (array[reminder], optional) 
+ `entityRelations` (array[Related item], optional) 
+ `resolutions` (array[string], optional) 
+ `resolutionSupport`: `{"approval":true,"acknowledgement":true,"esign":false}` (boolean, optional) - List of supported resolution methods, which could be initiated on this entity
+ `workflows` (array[string], optional) 


## Related item with expanded fields (object)


### Properties
+ `entity` (string, optional) 


## Related item with expanded fields2 (object)


### Properties
+ `fileUid` (string, required, nullable) 
+ `entityUid`: `Abc_123-XY` (string, required) 
+ `user` (Other user, required) 
+ `createdAt`: `1537177837` (number, required) - UNIX timestamp
+ `type` (enum[string], required) 
    + `folder`
    + `file`
    + `link`
+ `entity` (string, optional) 


## duplicationoptions (object)


### Properties
+ `filePeople`: `true` (boolean, optional) - Include file people/signees
+ `files`: `true` (boolean, optional) - Include files inside folders
+ `meta`: `true` (boolean, optional) - Include meta
+ `relations`: `false` (boolean, optional) - Include relations
+ `reminders`: `false` (boolean, optional) - Include reminders
+ `resolutions`: `false` (boolean, optional) - Include resolutions
+ `shares`: `false` (boolean, optional) - Include shares
+ `tags`: `true` (boolean, optional) - Include tags
+ `workflows`: `false` (boolean, optional) - Include workflows


## duplicate (object)


### Properties
+ `entities` (array[string], required) 
+ `locationUid` (string, required) 
+ `options` (duplicationoptions, optional) 


## duplicationStatus (object)


### Properties
+ `status` (enum[string], required) 
    + `success`
    + `active`
    + `failure`
+ `progress` (number, required) 
+ `total` (number, required) 
+ `failedEntity`: `Abc_123-XY` (string, optional) 
+ `duplicates` (array[string], optional) - Contains a list of duplicated entities in the original order


## errorResponse (object)


### Properties
+ `name` (string, optional) - Error name
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (number, optional) - Error status code


## fileUploadCreateBase (object)


### Properties
+ `action`: `create` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `contentType` (string, required) 
+ `parts` (object, optional) - Keys are part numbers, values are content lengths (number type)


## File1 (object)


### Properties
+ `action`: `create` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `contentType` (string, required) 
+ `parts` (object, optional) - Keys are part numbers, values are content lengths (number type)
+ `folderUid`: `Abc_123-XY` (string, required) 


## File version1 (object)


### Properties
+ `action`: `create` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `contentType` (string, required) 
+ `parts` (object, optional) - Keys are part numbers, values are content lengths (number type)
+ `fileUid`: `Abc_123-XY` (string, required) 


## Archive unpack (object)


### Properties
+ `action`: `create` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `contentType` (string, required) 
+ `parts` (object, optional) - Keys are part numbers, values are content lengths (number type)
+ `folderUid`: `Abc_123-XY` (string, required) 
+ `unpack` (Unpack, required) - Unpacking options


## Part (object)


### Properties
+ `action`: `part` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 
+ `partNumber` (number, required) 
+ `contentLength` (number, required) 


## Abort (object)


### Properties
+ `action`: `abort` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 


## fileUploadCompleteBase (object)


### Properties
+ `action`: `complete` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 


## File2 (object)


### Properties
+ `action`: `complete` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `folderUid`: `Abc_123-XY` (string, required) 


## File version2 (object)


### Properties
+ `action`: `complete` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `fileUid`: `Abc_123-XY` (string, required) 


## Archive unpack1 (object)


### Properties
+ `action`: `complete` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 
+ `fileSize` (number, required) 
+ `fileName` (string, required) 
+ `folderUid`: `Abc_123-XY` (string, required) 
+ `unpack` (Unpack, required) - Unpacking options


## Uploaded parts (object)


### Properties
+ `action`: `parts` (string, required) 
+ `uploadId` (string, required) 
+ `key` (string, required) 


## propertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canUpdateContent`: `true` (boolean, optional) - Add/manage versions
+ `canUpdateOnline`: `true` (boolean, optional) - Edit file in the cloud
+ `canDelete`: `true` (boolean, optional) 
+ `canDownload`: `true` (boolean, optional) 
+ `canPreview`: `true` (boolean, optional) 
+ `canLock`: `true` (boolean, optional) 
+ `canUpdateRetentionEnd`: `true` (boolean, optional) 
+ `canAudit`: `true` (boolean, optional) 


## File meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `metaUid`: `Abc_123-XY` (string, optional) 
+ `value`: `meta value` (string, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp


## Person1 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `Johnny Smith` (string, optional) 


## Tag1 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `myTag` (string, optional) 


## File3 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `folderUid`: `Abc_123-XY` (string, optional) 
+ `versionUid`: `Abc_123-XY` (string, optional) 
+ `name`: `myFile.txt` (string, optional) 
+ `extension`: `txt` (string, optional) 
+ `mimeType`: `text/plain` (string, optional) 
+ `size`: `123` (number, optional) 
+ `number`: `Sample 1.1` (string, optional, nullable) 
+ `date` (string, optional, nullable) 
+ `dueDate` (string, optional, nullable) 
+ `note`: `My note` (string, optional, nullable) 
+ `isSentByMail` (boolean, optional) 
+ `mailSender` (string, optional, nullable) 
+ `mailSenderName` (string, optional, nullable) 
+ `mailContent` (string, optional, nullable) 
+ `mailSubject` (string, optional, nullable) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `hash` (hash, optional) 
+ `language`: `eng` (string, optional) - OCR language
+ `meta` (array[File meta], optional) 
+ `people` (array[Person1], optional) 
+ `tags` (array[Tag1], optional) 


## folderpropertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 
+ `canDownload`: `true` (boolean, optional) 
+ `canCreateFolder`: `true` (boolean, optional) 
+ `canCreateFile`: `true` (boolean, optional) 
+ `canUpdateRetention`: `true` (boolean, optional) 
+ `canAudit`: `true` (boolean, optional) 


## grouppropertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 
+ `canViewMembers`: `true` (boolean, optional) 
+ `canAddMember`: `true` (boolean, optional) 


## Group member user (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - User UID
+ `name` (string, optional) - User name
+ `email`: `someEmail@foo.bar` (string, optional) - User email
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `type` (enum[string], optional) 
    + `user`
+ `permissions` (Permissions12, optional) 


## Group member child (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Group UID
+ `name`: `My group` (string, optional) - Group name
+ `type` (enum[string], optional) 
    + `child`
+ `permissions` (Permissions12, optional) 


## metaBasepropertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## Folder meta (object)


### Properties
+ `meta` (string, optional) 
+ `folderUid`: `Abc_123-XY` (string, optional) 
+ `isRequired`: `false` (boolean, optional) 
+ `isRecursive`: `false` (boolean, optional) 
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `includeFiles`: `true` (boolean, optional) 
+ `includeLinks`: `true` (boolean, optional) 
+ `includeFolders`: `false` (boolean, optional) 


## personpropertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## Plan (object)


### Properties
+ `id`: `1` (number, optional) 
+ `name`: `Free plan` (string, optional) 
+ `priceBase`: `10` (number, optional) 
+ `yearDiscount`: `0.9` (number, optional) 


## Search Result (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `My entity name` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `icon` (enum[string], optional) 
    + `folder`
    + `file`
    + `picture`
    + `chart`
    + `asic`
    + `link`
+ `score`: `1.5` (number, optional) 
+ `sort` (array[string], optional) 
+ `path`: `/someUid123/someUid124` (string, optional) - UID Path tree
+ `pathUid`: `/someUid123/someUid124` (string, optional) - UID Path tree
+ `mimeType` (string, optional, nullable) 
+ `createdAt` (string, optional) 


## Search Result with expanded fields (object)


### Properties
+ `entity` (string, optional) 
+ `folder` (string, optional, nullable) 
+ `file` (string, optional, nullable) 
+ `link` (Link, optional, nullable) 
+ `asic` (string, optional, nullable) 
+ `highlight` (string, optional) 


## Search Result with expanded fields2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `My entity name` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`
+ `icon` (enum[string], optional) 
    + `folder`
    + `file`
    + `picture`
    + `chart`
    + `asic`
    + `link`
+ `score`: `1.5` (number, optional) 
+ `sort` (array[string], optional) 
+ `path`: `/someUid123/someUid124` (string, optional) - UID Path tree
+ `pathUid`: `/someUid123/someUid124` (string, optional) - UID Path tree
+ `mimeType` (string, optional, nullable) 
+ `createdAt` (string, optional) 
+ `entity` (string, optional) 
+ `folder` (string, optional, nullable) 
+ `file` (string, optional, nullable) 
+ `link` (Link, optional, nullable) 
+ `asic` (string, optional, nullable) 
+ `highlight` (string, optional) 


## andOperator (object)
Require all items

### Properties
+ `andOperator` (array[string], required) 


## orOperator (object)
Require one of items

### Properties
+ `orOperator` (array[string], required) 


## notOperator (object)
Require item to not exist

### Properties
+ `notOperator` (string, required) 


## tagpropertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## sharepropertiespermissions (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## auditTrailFileEntryBase (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## Entity2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `name`: `My entity name` (string, optional) 
+ `type` (enum[string], optional) 
    + `folder`
    + `file`
    + `link`


## Audit trail tag (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Tag UID
+ `name`: `myTag` (string, optional) - Tag name


## Audit trail person (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Person UID
+ `name`: `Johnny Smith` (string, optional) - Person name


## auditTrailShare (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Share UID
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `groupUid`: `Abc_123-XY` (string, optional) - Group UID
+ `groupName`: `My group` (string, optional) - Group name
+ `userUid` (string, optional, nullable) - User UID
+ `email`: `someEmail@foo.bar` (string, optional) - Email
+ `public` (boolean, optional) 


## Audit trail file meta (object)


### Properties
+ `uid` (string, required) 
+ `metaUid` (string, required) 
+ `type` (enum[string], required) 
    + `string`
    + `text`
    + `integer`
    + `float`
    + `date`
    + `datetime`
    + `time`
    + `boolean`
    + `url`
    + `email`
    + `select`
+ `multiple` (boolean, required) 
+ `name` (string, required) 
+ `value`: `Abc_123-XY` (string, required) 
+ `decimalScale` (number, optional) 


## Audit trail file approval (object)
inviteId, invitee, inviteeId and text properties are only filled for approval responses

### Properties
+ `method` (enum[string], optional) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`
+ `name` (string, optional) 
+ `versionUid` (string, optional) 
+ `resolutionUid` (string, optional) 
+ `inviteUid` (string, optional) 
+ `invitee` (string, optional, nullable) 
+ `inviteeUid` (string, optional, nullable) 
+ `text` (string, optional, nullable) 
+ `comment` (string, optional, nullable) 


## auditTrailFolderEntryBase (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Audit trail folder meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - FolderMeta UID
+ `metaUid`: `Abc_123-XY` (string, optional) - Meta UID
+ `name`: `meta name` (string, optional) - Meta name
+ `isRequired`: `false` (boolean, optional, nullable) 
+ `isRecursive`: `false` (boolean, optional, nullable) 


## Audit trail folder automated workflow (object)


### Properties
+ `status` (enum[string], optional) 
    + `inherit`
    + `active`
    + `inactive`
+ `isRecursive`: `true` (boolean, optional) 
+ `type` (enum[string], optional) 
    + `parallel`
    + `serial`
+ `text` (string, optional) 


## Audit trail automated file numbering (object)


### Properties
+ `scheme`: `Document 1.{number}` (string, optional) 
+ `isRecursive`: `true` (boolean, optional) 
+ `name`: `This is my custom numbering` (string, optional, nullable) 
+ `resetType` (enum[string], optional) 
    + `never`
    + `yearly`
    + `monthly`
    + `weekly`
    + `daily`
+ `resetMonth`: `1` (number, optional, nullable) - Used with yearly reset
+ `resetDay`: `1` (number, optional, nullable)

    0 -> last day of month
    Used with yearly and monthly reset

+ `resetWeekday` (number, optional, nullable) - Used with weekly reset
+ `number` (number, optional) - Next number to be used
+ `applyFiles`: `true` (boolean, optional) - Apply numbering to files
+ `applyLinks`: `true` (boolean, optional) - Apply numbering to links
+ `applyFolders`: `true` (boolean, optional) - Apply numbering to folders


## auditTrailAccountEntryBase (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Audit trail meta (object)


### Properties
+ `metaUid`: `abcdefghij` (string, optional) 
+ `type` (enum[string], optional) - Meta type
    + `string`
    + `text`
    + `integer`
    + `float`
    + `date`
    + `datetime`
    + `time`
    + `boolean`
    + `url`
    + `email`
    + `select`
+ `multiple`: `false` (boolean, optional) - Meta multiple
+ `name`: `Owner` (string, optional) 
+ `description`: `Owner of the file` (string, optional, nullable) 


## Audit trail user (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - User UID
+ `email`: `someEmail@foo.bar` (string, optional) - User email


## Audit trail group (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Group UID
+ `name`: `My group` (string, optional) - Group name
+ `oldName` (string, optional, nullable) 


## Audit trail group user (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - User UID
+ `email`: `someEmail@foo.bar` (string, optional) - User email
+ `groupUid`: `Abc_123-XY` (string, optional) - Group UID
+ `groupName`: `My group` (string, optional) - Group name


## Audit trail group user1 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Child group UID
+ `name`: `My group` (string, optional) - Child group name
+ `groupUid`: `Abc_123-XY` (string, optional) - Group UID
+ `groupName`: `My group` (string, optional) - Group name


## File entries (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `event`: `fileNew` (string, required) 
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, required, nullable) 
+ `type`: `folder` (string, required) 
+ `data` (Data18, required) - Either source or target can be null, in which case it was or became a section


## resolutioninvite (object)


### Properties
+ `order` (number, optional) 
+ `email` (string, required) 
+ `userUid` (string, required) 
+ `groupUid` (string, required) 


## resolutioncreate (object)


### Properties
+ `type` (enum[string], required) 
    + `parallel`
    + `serial`
    + `custom`
+ `invites` (array[resolutioninvite], optional) 
+ `method` (enum[string], required) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`
+ `note` (string, optional) 
+ `showMeta` (boolean, optional) 
+ `showRelatedEntities` (boolean, optional) 
+ `timeoutTime` (string, optional) 


## resolutionupdate (object)


### Properties
+ `type` (enum[string], optional) 
    + `parallel`
    + `serial`
    + `custom`
+ `note` (string, optional) 
+ `showMeta` (boolean, optional) 
+ `showRelatedEntities` (boolean, optional) 
+ `timeoutTime` (string, optional) 


## Resolution invite1 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `order` (number, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) - User email
+ `userUid` (string, optional, nullable) 
+ `delegateUserUid` (string, optional, nullable) 
+ `status` (enum[string], optional) 
    + `pending`
    + `pendingInvite`
    + `active`
    + `approved`
    + `rejected`
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp


## Resolution invite with expanded fields (object)


### Properties
+ `user` (Other user, optional, nullable) 
+ `delegateUser` (Other user, optional, nullable) 


## Resolution invite with expanded fields2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `order` (number, optional) 
+ `email`: `someEmail@foo.bar` (string, optional) - User email
+ `userUid` (string, optional, nullable) 
+ `delegateUserUid` (string, optional, nullable) 
+ `status` (enum[string], optional) 
    + `pending`
    + `pendingInvite`
    + `active`
    + `approved`
    + `rejected`
+ `createdAt`: `1537177837` (number, optional) - UNIX timestamp
+ `updatedAt`: `1537177837` (number, optional) - UNIX timestamp
+ `user` (Other user, optional, nullable) 
+ `delegateUser` (Other user, optional, nullable) 


## inviteupdate (object)


### Properties
+ `order` (number, optional) 


## workflowcreate (object)


### Properties
+ `entityUid` (string, optional, nullable) 
+ `isActive` (boolean, optional) 
+ `isRecursive` (boolean, optional) 
+ `name` (string, optional, nullable) 
+ `steps` (array[string], optional) 
+ `triggers` (array[workflowtriggercreate], optional) 


## workflowupdate (object)


### Properties
+ `isActive` (boolean, optional) 
+ `isRecursive` (boolean, optional) 
+ `name` (string, optional, nullable) 
+ `steps` (array[string], optional) 


## workflowversioncreate (object)


### Properties
+ `steps` (array[string], required) 


## workflowversionupdate (object)


### Properties
+ `isActive` (boolean, optional) 


## workflowtriggercreate (object)


### Properties
+ `condition` (array[string], optional, nullable) 
+ `events` (array, required) 
    + (enum[string])
        + Members
            + `entityNew`
            + `entityDelete`
            + `entityRestore`
            + `entityMove`
            + `entityName`
            + `entityNote`
            + `entityDescription`
            + `entityNumber`
            + `entityDate`
            + `entityDueDate`
            + `entityMetaNew`
            + `entityMetaDelete`
            + `entityMetaValue`
            + `entityTagNew`
            + `entityTagDelete`
            + `entityRelationNew`
            + `entityRelationDelete`
            + `fileVersionNew`
            + `fileVersionDelete`
            + `fileVersionRestore`
            + `fileSigneeNew`
            + `fileSigneeDelete`
            + `folderLanguage`
            + `folderMetaUpdate`
            + `linkAddress`
            + `linkEmbedded`
            + `resolutionResponse`
            + `resolutionComplete`
            + `shareNew`
            + `shareDelete`
            + `shareUpdate`


## workflowtriggerupdate (object)


### Properties
+ `condition` (array[string], optional, nullable) 
+ `events` (array, optional) 
    + (enum[string])
        + Members
            + `entityNew`
            + `entityDelete`
            + `entityRestore`
            + `entityMove`
            + `entityName`
            + `entityNote`
            + `entityDescription`
            + `entityNumber`
            + `entityDate`
            + `entityDueDate`
            + `entityMetaNew`
            + `entityMetaDelete`
            + `entityMetaValue`
            + `entityTagNew`
            + `entityTagDelete`
            + `entityRelationNew`
            + `entityRelationDelete`
            + `fileVersionNew`
            + `fileVersionDelete`
            + `fileVersionRestore`
            + `fileSigneeNew`
            + `fileSigneeDelete`
            + `folderLanguage`
            + `folderMetaUpdate`
            + `linkAddress`
            + `linkEmbedded`
            + `resolutionResponse`
            + `resolutionComplete`
            + `shareNew`
            + `shareDelete`
            + `shareUpdate`


## 400badRequest1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Bad Request`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `400`


## 401unauthorized1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Unauthorized`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `401`


## 403forbidden1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Forbidden`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `403`


## 404notFound1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Not Found`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `404`


## 409conflict1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Conflict`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `409`


## 422unprocessableEntity1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Unprocessable entity`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `422`


## 500serverError1 (object)


### Properties
+ `name` (enum[string], optional) - Error name
    + `Internal Server Error`
+ `message`: `This is an error message explaining the issue` (string, optional) - Error message
+ `code` (number, optional) - Not used
+ `status` (enum[number], optional) - Error status code
    + `500`


## Account created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Account deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Account downloaded (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountDownload` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Account inbox toggled (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountInbox` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `false` (boolean, required) 


## Account indexing changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountIndexing` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `true` (boolean, required) 


## Account OCR language changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountLanguage` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `English` (string, required) 


## Account renamed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `New account name` (string, required) 


## Account sections reordered (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountSectionOrder` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Account shared (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `shareNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Account team folder toggled (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamFolder` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `false` (boolean, required) 


## Account watermarking toggled (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountWatermarking` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `false` (boolean, required) 


## Activate mailin address (object)


### Properties
+ `action` (enum[string], optional) 
    + `activate`


## Activated mailin address (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderEmailActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data15, required) 


## Active (object)


### Properties
+ `status` (enum[number], optional) - Unpacking currently active
    + `1`
+ `text` (enum[string], optional) 
    + `active`
+ `progress` (number, optional, nullable) 
+ `progressMax` (number, optional, nullable) 
+ `currentPath` (string, optional, nullable) 


## AddRemindersRequest (object)


### Properties
+ `content`: `Hello` (string, required) 
+ `remindAt`: `1537177837` (string, required) - Unix timestamp
+ `recurrence` (enum[string], optional, nullable) 
    + `daily`
    + `weekly`
    + `monthly`
    + `quarterly`
    + `semi-yearly`
    + `yearly`
+ `email`: `someEmail@foo.bar` (string, required) 
+ `groupUid`: `Abc_123-XY` (string, required) 


## Archive uncompressed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderUncompress` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `Archive name` (string, required) 


## Archive unpack2 (object)


### Properties
+ `key`: `GpW5kHBr` (string, optional) - Unpack key, check status from [`GET /accounts/{accountUid}/files/unpack-status/{key}`](#tag/File/operation/getUnpackStatus)


## Automated File Numbering disabled (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `numberingInherit` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Automated File Numbering enabled (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `numberingActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated File Numbering name changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `numberingName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated File Numbering next number changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `numberingNumber` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated File Numbering reset changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `numberingReset` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated File Numbering scheme changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `numberingScheme` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering activated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering deactivated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingDeactivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering inclusion of files changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingApplyFiles` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering inclusion of folders changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingApplyFolders` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering inclusion of links changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingApplyLinks` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering name changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering next number changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingNumber` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering recursion changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingRecursive` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering reset changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingReset` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering scheme changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingScheme` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail automated file numbering, required) 


## Automated Numbering set to inherit (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `numberingInherit` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Boolean (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 
+ `operator` (enum[string], optional) 
    + `is`
+ `value` (boolean, optional) 


## By email (object)


### Properties
+ `email`: `newMember@foo.bar` (string, required) 


## By groupUid (object)


### Properties
+ `groupUid`: `Abc_123-XY` (string, required) 


## By name (object)


### Properties
+ `name`: `Johnny Smith` (string, required) 


## By uid (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 


## By userUid (object)


### Properties
+ `userUid`: `Abc_123-XY` (string, required) 
+ `email`: `someEmail@foo.bar` (string, required) 


## By userUid1 (object)


### Properties
+ `userUid`: `Abc_123-XY` (string, required) 


## Conflicting file (object)


### Properties
+ `status` (enum[number], optional) - Found a conflicting file
    + `5`
+ `text` (enum[string], optional) 
    + `conflict`
+ `progress` (string, optional, nullable) 
+ `progressMax` (string, optional, nullable) 
+ `currentPath`: `./someFile.txt` (string, optional) 


## Create (object)


### Properties
+ `uploadId` (string, optional) 
+ `key` (string, optional) 


## Create with URLs (object)


### Properties
+ `uploadId` (string, optional) 
+ `key` (string, optional) 
+ `urls` (object, optional) - List of URLs, same as from the "part" action


## Created mailin address (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderEmailNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data15, required) 


## createdFrom (object)


### Properties
+ `createdFrom` (string, required) - From upload date


## createdTo (object)


### Properties
+ `createdTo` (string, required) - To upload date


## Data (object)


### Properties
+ `fileVersionUid` (string, optional, nullable) 
+ `email` (string, optional, nullable) 
+ `token` (string, optional, nullable) 


## Data2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - FileVersion UID
+ `name`: `sample.txt` (string, optional) - File name


## Data5 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Item UID
+ `name`: `My entity name` (string, optional) - Item name
+ `type` (enum[string], optional) - Item type
    + `folder`
    + `file`
    + `link`


## Data7 (object)


### Properties
+ `source` (Source folder, optional) 
+ `target` (Target folder, optional) 


## Data8 (object)


### Properties
+ `end`: `1537177837` (number, optional, nullable) - null value for both end and action results in forced infinite retention
+ `action` (enum[string], optional, nullable) 
    + `delete`
    + `purge`
    + `archive`
+ `custom` (boolean, optional) - Indicates whether the retentionEnd is overridden from the default
+ `target`: `Abc_123-XY` (string, optional) 


## Data9 (object)
Either source or target can be null, in which case it was or became a section

### Properties
+ `source` (Source folder, optional) 
+ `target` (Target folder, optional) 


## Data10 (object)
Target can be null, in which case it became a section

### Properties
+ `folder` (Folder that moved, optional) 
+ `target` (Target folder, optional) 


## Data11 (object)
Source can be null, in which case it was a section

### Properties
+ `folder` (Folder that moved, optional) 
+ `source` (Source folder, optional) 


## Data12 (object)


### Properties
+ `file` (File4, optional) 
+ `folder` (Folder1, optional) 


## Data14 (object)


### Properties
+ `time`: `1m5d` (string, optional)

    Retention time order can matter as the durations are processed in the order they are stored  
    For example:  
    1m5d -> time uploaded + 1 month + 5 days  
    5d1m -> time uploaded + 5 days + 1 month  
    Format accepts input with spaces between durations (accepts both "1m5d" and "1m 5d" are valid), excess whitespace is trimmed automatically  
    Set time to 0 length string and action to null to force infinite retention

+ `action` (enum[string], optional, nullable) 
    + `delete`
    + `purge`
    + `archive`
+ `isRecursive` (boolean, optional, nullable) 
+ `target`: `Abc_123-XY` (string, optional) 
+ `keepStructure` (boolean, optional) - Keep folder structure on archiving with isRecursive active


## Data15 (object)


### Properties
+ `address` (string, optional) 


## Data18 (object)
Either source or target can be null, in which case it was or became a section

### Properties
+ `uid`: `Abc_123-XY` (string, optional) - File UID
+ `name`: `My entity name` (string, optional) - File name
+ `source` (Source folder, optional) 
+ `target` (Target folder, optional) 


## Date (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 
+ `operator` (enum[string], optional) 
    + `is`
    + `lt`
    + `lte`
    + `gt`
    + `gte`
+ `value` (string, optional) 


## Date changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileDate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (string, required) 


## dateDueFrom (object)


### Properties
+ `dateDueFrom` (string, required) - From date due


## dateDueTo (object)


### Properties
+ `dateDueTo` (string, required) - To date due


## dateFrom (object)


### Properties
+ `dateFrom` (string, required) - From date


## dateOrCreatedFrom (object)


### Properties
+ `dateOrCreatedFrom` (string, required) - From date


## dateOrCreatedTo (object)


### Properties
+ `dateOrCreatedTo` (string, required) - To date


## Datetime (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 
+ `operator` (enum[string], optional) 
    + `is`
    + `lt`
    + `lte`
    + `gt`
    + `gte`
+ `value` (string, optional) 


## dateTo (object)


### Properties
+ `dateTo` (string, required) - To date


## Deactivate mailin address (object)


### Properties
+ `action`: `deactivate` (string, required) 


## Deactivated mailin address (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderEmailDeactivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data15, required) 


## Decimal (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `is`
    + `lt`
    + `lte`
    + `gt`
    + `gte`
+ `value`: `42.50` (string, optional) 


## Due date changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileDueDate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (string, required) 


## Duplicated file (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfileDuplicate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Duplicated subfolder (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfolderDuplicate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Email (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `has`
    + `is`
    + `prefix`
    + `wildcard`
+ `value` (string, optional) 


## Exists (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 
+ `operator` (enum[string], optional) 
    + `exists`


## extension (object)


### Properties
+ `extension`: `txt` (string, required) - Extension


## Extra (object)


### Properties
+ `number` (string, optional, nullable) 
+ `note` (string, optional, nullable) 
+ `date` (string, optional, nullable) 
+ `dueDate` (string, optional, nullable) 


## Failure (object)


### Properties
+ `status` (enum[number], optional) - Unpacking failed
    + `2`
+ `text` (enum[string], optional) 
    + `failure`
+ `progress` (string, optional, nullable) 
+ `progressMax` (string, optional, nullable) 
+ `currentPath` (string, optional, nullable) 


## File Approval approved (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileResolutionApproved` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file approval, required) - inviteId, invitee, inviteeId and text properties are only filled for approval responses


## File Approval cancelled (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileResolutionDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file approval, required) - inviteId, invitee, inviteeId and text properties are only filled for approval responses


## File Approval rejected (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileResolutionRejected` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file approval, required) - inviteId, invitee, inviteeId and text properties are only filled for approval responses


## File Approval response approved (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileResolutionResponseApproved` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file approval, required) - inviteId, invitee, inviteeId and text properties are only filled for approval responses


## File Approval response rejected (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileResolutionResponseRejected` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file approval, required) - inviteId, invitee, inviteeId and text properties are only filled for approval responses


## File Approval started (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileResolutionNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file approval, required) - inviteId, invitee, inviteeId and text properties are only filled for approval responses


## File deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File deleted1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfileDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## File downloaded (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileDownload` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data, required, nullable) 


## File duplicated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileDuplicate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Entity2, required) 


## File locked (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileLock` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File moved (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileMove` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data7, required) 


## File moved away (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfileMove` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data12, required) 


## File moved in (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfileMoveTarget` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data12, required) 


## File number changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileNumber` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (string, required, nullable) 


## File OCR language changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileLanguage` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data`: `English` (string, required) 


## File opened for online editing (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileOnlineEdit` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File opened for online viewing (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileOnlineView` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File preview downloaded (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `filePreviewDownload` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data, required, nullable) 


## File renamed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data`: `New file name` (string, required) 


## File retention changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileRetentionEnd` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data8, required) 


## File retention changed1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileRetentionEnd` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File shared (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `shareNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (auditTrailShare, required) 


## File too large (object)


### Properties
+ `status` (enum[number], optional) - File inside the archive is too large to unpack
    + `3`
+ `text` (enum[string], optional) 
    + `oversize`
+ `progress` (string, optional, nullable) 
+ `progressMax` (string, optional, nullable) 
+ `currentPath`: `./someFile.txt` (string, optional) 


## File unlocked (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileUnlock` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File uploaded (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`


## File4 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - File UID
+ `name`: `My entity name` (string, optional) - File name


## FileMeta created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileMetaRequired` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file meta, required) 


## FileMeta deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileMetaDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file meta, required) 


## FileMeta value changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileMetaValue` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail file meta, required) 


## filterItem (object)


### Properties
+ `tag` (string, required) - Tag


## filterItem1 (object)


### Properties
+ `person` (string, required) - Person


## filterItem2 (object)


### Properties
+ `number` (string, required) - Number
+ `operator` (enum[string], optional) 
    + `has`


## Folder Automated Workflow activated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder automated workflow, required) 


## Folder Automated Workflow deactivated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionDeactivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder automated workflow, required) 


## Folder Automated Workflow method changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionMethod` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder automated workflow, required) 


## Folder Automated Workflow recursion changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionRecursive` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder automated workflow, required) 


## Folder Automated Workflow set to inherit (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionInherit` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Folder Automated Workflow text changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionText` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder automated workflow, required) 


## Folder Automated Workflow type changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderResolutionType` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder automated workflow, required) 


## Folder created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Folder deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Folder downloaded (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderDownload` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Folder duplicated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderDuplicate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Folder moved (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderMove` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data9, required) - Either source or target can be null, in which case it was or became a section


## Folder OCR language changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderLanguage` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `English` (string, required) 


## Folder rename (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data`: `New folder name` (string, required) 


## Folder retention changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderRetention` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data14, required) 


## Folder shared (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `shareNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Folder that moved (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Folder UID
+ `name`: `My folder name` (string, optional) - Folder name


## Folder visible columns changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderVisibleColumns` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Folder1 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Folder UID
+ `name`: `My folder name` (string, optional) - Folder name


## Folder3 (object)


### Properties
+ `name`: `My folder name` (string, required) 
+ `visibleColumns` (array[string], optional) - Array of columns to be shown by the client
+ `language`: `eng` (string, optional) - OCR language
+ `parentUid`: `Abc_123-XY` (string, required) 


## FolderMeta created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderMetaNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder meta, required) 


## FolderMeta deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderMetaDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder meta, required) 


## FolderMeta recursive changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderMetaRecursive` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder meta, required) 


## FolderMeta required changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderMetaRequired` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail folder meta, required) 


## Generic action (object)


### Properties
+ `action` (string, required) 


## Group created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupAdd` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group, required) 


## Group deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group, required) 


## Group member child added (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupChildAdd` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group user1, required) 


## Group member child removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupChildDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group user1, required) 


## Group member user added (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupUserAdd` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group user, required) 


## Group member user removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupUserDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group user, required) 


## Group renamed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountGroupName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail group, required) 


## hasRelations (object)


### Properties
+ `hasRelations` (boolean, required) - Find files that have or don't have related files set


## Integer (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `is`
    + `lt`
    + `lte`
    + `gt`
    + `gte`
+ `value`: `42` (number, optional) 


## language (object)


### Properties
+ `language`: `eng` (string, required) - OCR language


## Locked file (object)


### Properties
+ `status` (enum[number], optional) - Found a locked file
    + `6`
+ `text` (enum[string], optional) 
    + `locked`
+ `progress` (string, optional, nullable) 
+ `progressMax` (string, optional, nullable) 
+ `currentPath`: `./someFile.txt` (string, optional) 


## mailContent (object)


### Properties
+ `mailContent` (string, required) - Mail Content


## mailSenderEmail (object)


### Properties
+ `mailSenderEmail` (string, required) - Mail Sender e-mail


## mailSenderName (object)


### Properties
+ `mailSenderName` (string, required) - Mail Sender Name


## mailSubject (object)


### Properties
+ `mailSubject` (string, required) - Mail Subject


## Meta (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Meta UID
+ `name` (string, optional) - Meta name


## Meta Column (object)


### Properties
+ `meta`: `Abc_123-XY` (string, optional) - Meta UID


## Meta Column1 (object)


### Properties
+ `meta` (Meta, optional) 


## Meta created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `metaNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail meta, required) 


## Meta deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `metaDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail meta, required) 


## Meta description changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `metaDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail meta, required) 


## Meta name changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `metaName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail meta, required) 


## meta1 (object)


### Properties
+ `meta` (array[string], required) - Include meta


## New file (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfileNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## New subfolder (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfolderNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Note changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileNote` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data`: `New file note` (string, required) 


## Other user1 (object)
Indicates that a substitute user performed the action

### Properties
+ `uid`: `Abc_123-XY` (string, optional) - User UID
+ `name` (string, optional) - User name
+ `email`: `someEmail@foo.bar` (string, optional) - User email
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 


## Out of space (object)


### Properties
+ `status` (enum[number], optional) - Account does not have enough space to uncompress the archive
    + `4`
+ `text` (enum[string], optional) 
    + `freespace`
+ `progress` (string, optional, nullable) 
+ `progressMax` (string, optional, nullable) 
+ `currentPath` (string, optional, nullable) 


## Params (object)


### Properties
+ `destination` (string, required) 


## Params1 (object)


### Properties
+ `type` (enum[string], required) 
    + `parallel`
    + `serial`
    + `custom`
+ `invites` (array[resolutioninvite], required) 
+ `keepActive` (boolean, optional) 
+ `method` (enum[string], required) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`
+ `note` (string, optional) 
+ `showMeta` (boolean, optional) 
+ `showRelatedEntities` (boolean, optional) 
+ `timeout` (string, optional) 


## Params2 (object)


### Properties
+ `invites` (array[resolutioninvite], required) 
+ `method` (enum[string], required) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`
+ `timeout` (string, optional) 


## Params3 (object)


### Properties
+ `method` (enum[string], required) 
    + `approval`
    + `DocuSign`
    + `eID`
    + `acknowledgement`
    + `eSign`
    + `review`


## Params4 (object)


### Properties
+ `amount` (number, required) - Amount of seconds to delay


## parentUid (object)


### Properties
+ `parentUid`: `someUid123` (string, required) - Search from this parent


## Part data (object)


### Properties
+ `partNumber` (number, optional) 
+ `contentLength` (number, optional) 
+ `lastModified`: `1537177837` (number, optional) 


## Part1 (object)


### Properties
+ `url` (string, optional) - Send file data as PUT request to this url


## Permissions9 (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 
+ `canShare`: `true` (boolean, optional) 
+ `canUpdateRetention`: `true` (boolean, optional) 
+ `canAudit`: `true` (boolean, optional) 


## Permissions10 (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canUpdate`: `true` (boolean, optional) 
+ `canAddInvites`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## Permissions12 (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## Person added (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileSigneeNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail person, required) 


## Person created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `signeeNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail tag, required) 


## Person deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `signeeDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail tag, required) 


## Person name changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `signeeName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail tag, required) 


## Person removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileSigneeDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail person, required) 


## Person2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 


## propertiessort (object)


### Properties
+ `field` (enum[string], optional) 
    + `score`
    + `id`
    + `uid`
    + `name`
    + `size`
    + `number`
    + `date`
    + `dueDate`
    + `mailSender`
    + `mailSenderName`
    + `mailSubject`
    + `extension`
    + `mimeType`
    + `address`
    + `meta:<uid>`
+ `order` (enum[string], optional) 
    + `asc`
    + `desc`


## query (object)


### Properties
+ `query` (string, required) 
+ `queryFilter` (array, optional) - Query filter
    + (enum[string])
        + Members
            + `id`
            + `uid`
            + `name`
            + `meta`
            + `tag`
            + `person`
            + `content`
            + `note`
            + `number`
            + `mailSubject`
            + `mailSenderEmail`
            + `mailSenderName`
            + `mailContent`


## Related item added (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileRelationNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data5, required) 


## Related item removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileRelationDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data5, required) 


## relatedUid (object)


### Properties
+ `relatedUid` (array[string], required) - Return only the ones with specified related file UIDs


## Reset mailin address (object)


### Properties
+ `action`: `reset` (string, required) 


## Resolution1 (object)


### Properties
+ `status` (enum[string], optional) 
    + `deleted`
    + `preparing`
    + `active`
    + `inProgress`
    + `complete`
    + `rejected`
+ `uid` (string, optional) 


## Section (object)


### Properties
+ `name`: `My folder name` (string, required) 
+ `visibleColumns` (array[string], optional) - Array of columns to be shown by the client
+ `language`: `eng` (string, optional) - OCR language


## Section created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountSectionNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Section deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountSectionDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Section duplicated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountSectionDuplicate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Select (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `is`
+ `value`: `Abc_123-XY` (string, optional) 


## Share activated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `shareActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (auditTrailShare, required) 


## Share activated1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `shareActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share activated2 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `shareActivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share deactivated (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `shareDeactivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (auditTrailShare, required) 


## Share deactivated1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `shareDeactivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share deactivated2 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `shareDeactivate` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share expiration changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `shareExpiration` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (auditTrailShare, required) 


## Share expiration changed1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `shareExpiration` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share expiration changed2 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `shareExpiration` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `shareDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (auditTrailShare, required) 


## Share removed1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `shareDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share removed2 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `shareDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share role changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `shareRole` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (auditTrailShare, required) 


## Share role changed1 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `shareRole` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Share role changed2 (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `shareRole` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (auditTrailShare, required) 


## Source folder (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Folder UID
+ `name`: `My folder name` (string, optional) - Folder name


## String (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `has`
    + `is`
    + `prefix`
    + `wildcard`
+ `value`: `meta value` (string, optional) 


## Subfolder deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfolderDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Entity2, required) 


## Subfolder moved away (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfolderMove` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data10, required) - Target can be null, in which case it became a section


## Subfolder moved in (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `folder` (string, required) 
+ `event`: `folderSubfolderMoveTarget` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Folder UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Data11, required) - Source can be null, in which case it was a section


## Success (object)


### Properties
+ `status` (enum[number], optional) - Unpacking finished
    + `0`
+ `text` (enum[string], optional) 
    + `success`
+ `progress`: `5` (number, optional) 
+ `progressMax`: `25` (number, optional) 
+ `currentPath` (string, optional, nullable) 


## Supports (object)


### Properties
+ `versioning` (boolean, optional) - Indicates whether file support versioning with version, versionUid and previewAvailable
+ `locking` (boolean, optional) - Indicates whether file supports locking


## Tag added (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileTagNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail tag, required) 


## Tag created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `tagNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail tag, required) 


## Tag deleted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `tagDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail tag, required) 


## Tag name changed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `tagName` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail tag, required) 


## Tag removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileTagDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Audit trail tag, required) 


## Tag2 (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 


## Tag3 (object)


### Properties
+ `name`: `myTag` (string, required) 


## tagUid (object)


### Properties
+ `tagUid` (array[string], required) - Required tag uid list


## Target folder (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) - Folder UID
+ `name`: `My folder name` (string, optional) - Folder name


## Team user accepted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserAccept` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Team user adoption accepted (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserManagedAccept` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Team user adoption invitation (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserManagedInvite` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail user, required) 


## Team user adoption invitation removal (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserManagedInviteDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail user, required) 


## Team user adoption rejected (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserManagedReject` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Team user created (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserAdd` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail user, required) 


## Team user rejected (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserReject` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 


## Team user removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `account` (string, required) 
+ `event`: `accountTeamUserDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - Account UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `data` (Audit trail user, required) 


## Text (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `has`
    + `is`
    + `prefix`
    + `wildcard`
+ `value`: `meta value` (string, optional) 


## Time (object)


### Properties
+ `uid`: `Abc_123-XY` (string, required) 
+ `operator` (enum[string], optional) 
    + `is`
    + `lt`
    + `lte`
    + `gt`
    + `gte`
+ `value`: `17:32:28` (string, optional) 


## Unpack (object)
Unpacking options

### Properties
+ `overwrite`: `false` (boolean, required) - Overwrite existing files (add new version)
+ `newFolder`: `true` (boolean, required) - Create a new folder for the archive


## URL (object)


### Properties
+ `uid`: `Abc_123-XY` (string, optional) 
+ `operator` (enum[string], optional) 
    + `has`
    + `is`
    + `prefix`
    + `wildcard`
+ `value` (string, optional) 


## V2 Accounts Entities Action Duplicate Response (object)


### Properties
+ `url` (string, optional) 


## V2 Accounts Entities Meta MetaUid EntityMetaUid Request (object)


### Properties
+ `value` (string, optional, nullable) 


## V2 Accounts Entities Meta MetaUid Request (object)


### Properties
+ `value` (string, optional, nullable) 


## V2 Accounts Entities Meta Request (object)


### Properties
+ `metaUid`: `Abc_123-XY` (string, required) 
+ `value` (string, required, nullable) 


## V2 Accounts Entities Related Request (object)


### Properties
+ `entityUid`: `Abc_123-XY` (string, required) 


## V2 Accounts Entities Request (object)


### Properties
+ `name`: `My entity name` (string, optional) 
+ `parentUid` (string, optional, nullable) 
+ `retention` (retention, optional, nullable) - Recycle/Permanently delete/Archive items after x time, counting from the time uploaded
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting

+ `extra` (Extra, optional) 
+ `sectionOrder`: `0` (number, optional) - User-configurable value for manually ordering sections


## V2 Accounts Entities Restore Request (object)


### Properties
+ `parentUid` (string, optional, nullable) 


## V2 Accounts Entities Tags Request (object)


### Properties
+ `tags` (array[string], required) 


## V2 Accounts Files Download Response (object)


### Properties
+ `url` (string, optional) 


## V2 Accounts Files Lock Request (object)


### Properties
+ `time` (number, optional, nullable) - When supplied, either a specific end time for lock or null for infinite lock duration
+ `lockId` (string, optional, nullable) 


## V2 Accounts Files Meta MetaUid FileMetaUid Request (object)


### Properties
+ `value` (string, optional, nullable) 


## V2 Accounts Files Meta MetaUid Request (object)


### Properties
+ `value` (string, optional, nullable) 


## V2 Accounts Files Meta Request (object)


### Properties
+ `metaUid`: `Abc_123-XY` (string, required) 
+ `value` (string, required, nullable) 


## V2 Accounts Files People Request (object)


### Properties
+ `people` (array[string], required) 


## V2 Accounts Files Preview Response (object)


### Properties
+ `url` (string, optional) 


## V2 Accounts Files Related Request (object)


### Properties
+ `fileUid`: `Abc_123-XY` (string, required) 


## V2 Accounts Files Request (object)


### Properties
+ `name`: `My entity name` (string, optional) 
+ `date` (string, optional, nullable) 
+ `dueDate` (string, optional, nullable) 
+ `note` (string, optional, nullable) 
+ `number` (string, optional, nullable) 
+ `versionUid`: `Abc_123-XY` (string, optional) 
+ `folderUid`: `Abc_123-XY` (string, optional) 
+ `retentionEnd` (retentionEnd, optional, nullable)

    Recycle/permanently delete/archive entity at end time  
    Setting retentionEnd object to null restores the default "inherit from parent" setting



## V2 Accounts Files Restore Request (object)


### Properties
+ `folderUid`: `Abc_123-XY` (string, optional) 


## V2 Accounts Files Shares Request (object)


### Properties
+ `groupUid`: `Abc_123-XY` (string, required) 
+ `userUid`: `Abc_123-XY` (string, required) 
+ `email`: `someEmail@foo.bar` (string, required) 
+ `role` (enum[string], required) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `settings` (Share settings, required) 
+ `public`: `True` (string, required) 
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp


## V2 Accounts Files Tags Request (object)


### Properties
+ `tags` (array[string], required) 


## V2 Accounts Files Unlock Request (object)


### Properties
+ `lockId` (string, optional, nullable) 


## V2 Accounts Files Unpack Request (object)


### Properties
+ `overwrite`: `false` (boolean, required) - Overwrite existing files (add new version)
+ `newFolder`: `true` (boolean, required) - Create a new folder for the archive


## V2 Accounts Files Unpack Response (object)


### Properties
+ `key`: `GpW5kHBr` (string, optional) 


## V2 Accounts Files Versions FileVersionUid Download Response (object)


### Properties
+ `url` (string, optional) 


## V2 Accounts Files Versions FileVersionUid Preview Response (object)


### Properties
+ `url` (string, optional) 


## V2 Accounts Folders Meta MetaUid Request (object)


### Properties
+ `isRequired`: `false` (boolean, optional) 
+ `isRecursive`: `false` (boolean, optional) 
+ `includeFiles`: `true` (boolean, optional) 
+ `includeLinks`: `true` (boolean, optional) 
+ `includeFolders`: `false` (boolean, optional) 


## V2 Accounts Folders Meta Request (object)


### Properties
+ `metaUid`: `Abc_123-XY` (string, required) 
+ `isRequired`: `false` (boolean, optional) 
+ `isRecursive`: `false` (boolean, optional) 
+ `includeFiles`: `true` (boolean, optional) 
+ `includeLinks`: `true` (boolean, optional) 
+ `includeFolders`: `false` (boolean, optional) 


## V2 Accounts Folders Related Request (object)


### Properties
+ `entityUid`: `Abc_123-XY` (string, required) 


## V2 Accounts Folders Request (object)


### Properties
+ `name`: `My folder name` (string, optional) 
+ `parentUid` (string, optional, nullable) 
+ `visibleColumns` (array[string], optional) - Array of columns to be shown by the client
+ `retention` (retention, optional, nullable) - Recycle/Permanently delete/Archive items after x time, counting from the time uploaded
+ `extra` (Extra, optional) 
+ `sectionOrder`: `0` (number, optional) - User-configurable value for manually ordering sections
+ `sort`: `name` (string, optional) - Column names, prefixed with a dash (`-`) for descending order, separated with a comma
+ `viewType` (enum[string], optional) - View type of a folder listing
    + `list`
    + `thumbnail`


## V2 Accounts Folders Restore Request (object)


### Properties
+ `parentUid` (string, optional, nullable) 


## V2 Accounts Folders Shares Request (object)


### Properties
+ `groupUid`: `Abc_123-XY` (string, required) 
+ `userUid`: `Abc_123-XY` (string, required) 
+ `email`: `someEmail@foo.bar` (string, required) 
+ `role` (enum[string], required) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `UPLOAD`
    + `CUSTOM`
+ `settings` (Share settings, required) 
+ `public`: `True` (string, required) 
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp


## V2 Accounts Groups Request (object)


### Properties
+ `name`: `My group` (string, required) 


## V2 Accounts Groups Users UserUid Permissions Response (object)


### Properties
+ `canView`: `true` (boolean, optional) 
+ `canDelete`: `true` (boolean, optional) 


## V2 Accounts Meta Options OptionUid Request (object)


### Properties
+ `value`: `meta name` (string, required) 
+ `order`: `1` (number, optional) 


## V2 Accounts Meta Options Request (object)


### Properties
+ `value`: `meta name` (string, required) 
+ `order`: `1` (number, optional) 


## V2 Accounts Meta Request (object)


### Properties
+ `name`: `meta name` (string, required) 
+ `description` (string, optional, nullable) 
+ `type` (enum[string], optional) 
    + `string`
    + `text`
    + `integer`
    + `float`
    + `date`
    + `datetime`
    + `time`
    + `boolean`
    + `url`
    + `email`
    + `select`
+ `multiple`: `false` (boolean, optional) 


## V2 Accounts People Request (object)


### Properties
+ `name`: `Johnny Smith` (string, required) 


## V2 Accounts Reminders Request (object)


### Properties
+ `email`: `someEmail@foo.bar` (string, optional) 
+ `groupUid`: `Abc_123-XY` (string, optional) 
+ `content`: `Hello` (string, optional) 
+ `remindAt`: `1537177837` (string, optional) - Unix timestamp
+ `recurrence` (enum[string], optional, nullable) 
    + `daily`
    + `weekly`
    + `monthly`
    + `quarterly`
    + `semi-yearly`
    + `yearly`


## V2 Accounts Request (object)


### Properties
+ `name`: `Account name` (string, required) 
+ `indexing`: `true` (boolean, optional) 
+ `language`: `eng` (string, optional) - OCR language


## V2 Accounts Search Entities Response (object)


### Properties
+ `hits`: `1` (number, optional) 
+ `entities` (array[string], optional) 


## V2 Accounts Search Files Response (object)


### Properties
+ `hits`: `1` (number, optional) 
+ `files` (array[string], optional) 


## V2 Accounts Search Folders Response (object)


### Properties
+ `hits`: `1` (number, optional) 
+ `folders` (array[string], optional) 


## V2 Accounts Search Simple Response (object)


### Properties
+ `folders` (array[string], optional) 
+ `files` (array[string], optional) 


## V2 Accounts Shared Items Response (object)


### Properties
+ `folders` (array[string], optional) 
+ `files` (array[string], optional) 


## V2 Accounts Shares Request (object)


### Properties
+ `groupUid`: `Abc_123-XY` (string, required) 
+ `userUid`: `Abc_123-XY` (string, required) 
+ `email`: `someEmail@foo.bar` (string, required) 
+ `role` (enum[string], required) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `settings` (Share settings, required) 
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp


## V2 Accounts Tags Request (object)


### Properties
+ `name`: `myTag` (string, required) 
+ `parentUid` (string, optional, nullable) 


## V2 Accounts Tags Shares Request (object)


### Properties
+ `groupUid`: `Abc_123-XY` (string, required) 
+ `userUid`: `Abc_123-XY` (string, required) 
+ `email`: `someEmail@foo.bar` (string, required) 
+ `role` (enum[string], required) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `settings` (Share settings, required) 
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp


## V2 Accounts Unpack Check Request (object)


### Properties
+ `name`: `test.zip` (string, required) 


## V2 Accounts Users Request (object)


### Properties
+ `position` (string, optional, nullable) 


## V2 User Permissions Response (object)


### Properties
+ `canCreateAccount`: `true` (boolean, optional) 


## V2 User Request (object)


### Properties
+ `title`: `Mr.` (string, optional, nullable) 
+ `firstName`: `John` (string, optional, nullable) 
+ `lastName`: `Smith` (string, optional, nullable) 
+ `phone`: `+123 456 7890` (string, optional, nullable) 
+ `defaultAccountUid`: `Abc_123-XY` (string, optional) 
+ `locale`: `en-GB` (string, optional) 
+ `timezone`: `Europe/London` (string, optional) 


## V2 Accounts Request1 (object)


### Properties
+ `name`: `Account name` (string, optional) 
+ `indexing`: `true` (boolean, optional) 
+ `language`: `eng` (string, optional) - OCR language
+ `applyLanguageRecursive` (boolean, optional) - Apply account language to all child items
+ `applyLanguageFiles` (boolean, optional) - Apply account language to all files
+ `applyLanguageFolders` (boolean, optional) - Apply account language to all folders


## V2 Accounts Files Related Request1 (object)


### Properties
+ `entityUid`: `Abc_123-XY` (string, required) 


## V2 Accounts Meta Request1 (object)


### Properties
+ `name`: `meta name` (string, optional) 
+ `description` (string, optional, nullable) 
+ `multiple`: `false` (boolean, optional) 


## V2 Accounts Tags Request1 (object)


### Properties
+ `name`: `myTag` (string, optional) 
+ `parentUid` (string, optional, nullable) 


## V2 Accounts Shares Request1 (object)


### Properties
+ `role` (enum[string], optional) 
    + `READ`
    + `EDIT`
    + `PREVIEW`
    + `CUSTOM`
+ `status` (enum[string], optional) 
    + `ACTIVE`
    + `DEACTIVATED`
+ `expiration`: `1537177837` (number, optional, nullable) - UTC timestamp
+ `settings` (Share settings, optional) 


## Version added (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileVersionNew` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data2, required) 


## Version removed (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileVersionDelete` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data2, required) 


## Version restored (object)


### Properties
+ `id`: `1` (number, required) 
+ `uid`: `Abc_123-XY` (string, required) 
+ `endpoint` (enum[string], required) 
    + `frontend`
    + `api`
+ `user` (Other user, required, nullable) 
+ `activeUser` (Other user1, optional, nullable) 
+ `ip` (string, required) 
+ `type`: `file` (string, required) 
+ `event`: `fileVersionRestore` (string, required) 
+ `entityUid`: `Abc_123-XY` (string, required) - File UID
+ `accountUid`: `Abc_123-XY` (string, required) - Account UID
+ `time`: `1537177837` (number, required) 
+ `requestKey` (string, optional) 
+ `cause` (enum[string], optional) - Used for marking actions performed by background processes
    + `retention`
+ `data` (Data2, required) 

